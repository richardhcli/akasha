# Task status

Machine-checkable status for every task in `docs/build-plan.md` (M0–M10, 72
tasks total). This file is the single source of truth for "what's done" —
an autonomous agent picking up work should read this file first, find the
next `TODO` task whose `Depends on` tasks are all `DONE`, and work it per
the rules in `docs/build-plan.md` §"How to use this plan" and root
`CLAUDE.md`.

Status values: `TODO` → `IN PROGRESS` → `DONE`, or `BLOCKED: <reason>`.
A milestone is closed only when every task in it is `DONE` and the
milestone's own DoD command (stated in `docs/build-plan.md`) passes.

When you finish a task: flip its status here in the same change that closes
the task, and note anything a future agent needs (e.g. a new
`docs/spec-questions.md` entry) in the Notes column.

---

## M0 — Scaffold (Depends on: nothing) — **CLOSED**

Milestone DoD: `make check` green on windows-latest and ubuntu-latest with a
placeholder test. Verified locally on ubuntu (uv run ruff/pyright/pytest);
CI matrix not yet run (no remote configured).

| Task | Goal | Status | Notes |
|---|---|---|---|
| T0.1 | Create repository skeleton | DONE | |
| T0.2 | Author `pyproject.toml` (uv, Python ≥3.12) | DONE | |
| T0.3 | Migration runner | DONE | placeholder migration `000_placeholder.sql`; `001_init.sql` is T1.1's job |
| T0.4 | Ruff + pyright + custom bans wired | DONE | scoped ruff `S` selection to S102/S301/S302/S307 only, not full bandit set |
| T0.5 | Config loading (`config.py`) | DONE | |
| T0.6 | Structured JSON logging | DONE | landed in `daemon.py` per spec's suggested location |
| T0.7 | Makefile targets | DONE | `make` itself isn't installed in this sandbox; targets verified by running their `uv run ...` bodies directly |
| T0.8 | CI matrix (GitHub Actions) | DONE | YAML validated locally; not yet run against a remote |

## M2 — IDs + canonicalization (Depends on: M0) — **CLOSED**

Milestone DoD: `pytest tests/unit/test_ids.py tests/golden/test_serialization.py`
→ 33 passed. Hypothesis round-trip (`tests/property/test_canonical_idempotent.py`)
→ 2 passed, no falsifying example. Verified locally on ubuntu.

| Task | Goal | Status | Notes |
|---|---|---|---|
| T2.1 | ID minting + checksum + validation (`ids.py`) | DONE | Sonnet worker recovered after initial orchestration halt; 14 tests passing (includes 5 known checksum vectors, validation, minting, anchors). Confirmed via `make check`: ruff/pyright clean, 41 total tests pass. |
| T2.2 | Text + JSON canonicalization (`canonical.py`) | DONE | Sonnet worker created both canonical.py and test_canonical.py; 14 tests passing (CRLF/LF, NFC, fence detection, tabs, JSON hashing). Confirmed via `make check`: ruff/pyright clean, 41 total tests pass. |
| T2.3 | Canonicalization idempotence (property test) | DONE | Ran via redesigned fleet-workflow.js pipeline (docs/agents/logs/20260711-173459-M1-M2-mixed/). Hypothesis property test, 2 passed. Independently CONFIRMED_DONE by separate verifier agent (re-ran pytest itself). M2 milestone now CLOSED — T2.1-T2.4 all DONE. |
| T2.4 | Golden serialization corpus (≥15 cases) | DONE | Same run. 18 golden cases (>=15 required), 19 tests passed. Independently CONFIRMED_DONE — verifier checked all 37 files exist/non-empty (empty_file/input.md correctly 0 bytes by design) and cross-referenced git status. |

## M1 — Kernel store (Depends on: M0) — **CLOSED**

Milestone DoD: `pytest tests/unit/kernel tests/property/test_store.py` → 92 passed;
property suite covers no-dangling-refs, head-reachable, as-of, S0-GC-safety;
10k-node benchmark `tests/integration/test_perf.py::test_neighborhood_p95` → p95 < 50 ms
(1 passed in ~3.5s). Verified locally on ubuntu via `make check` equivalent:
ruff clean, pyright --strict 0 errors, 132 unit+property tests pass. All T1.3–T1.9
worker results independently verified (CONFIRMED_DONE) AND separately audited against
spec §4.1/§4.2/§4.4/§4.5/§4.6 by the Opus orchestrating session. Run log:
docs/agents/logs/20260711-215704-M1-kernel-store/. SPEC-QUESTIONs T1.3/T1.5/T1.6/T1.7
all RESOLVED 2026-07-11 (see docs/spec-questions.md): every narrowest reading was
confirmed correct against vision.md + mvp-spec.md; no code changes required. Follow-ups
noted there for M4 (vet endpoint must recompute maturity in-txn; history order is frozen
oldest-first) and M7 (T7.6 reassignment queue layers on the mechanical redirect default;
T7.2 maps facet-adds to 'minor').

| Task | Goal | Status | Notes |
|---|---|---|---|
| T1.1 | DDL migration `001_init.sql` (verbatim) | DONE | Same run as T2.3/T2.4 (docs/agents/logs/20260711-173459-M1-M2-mixed/). 5 schema tests passed. Independently CONFIRMED_DONE. |
| T1.2 | pydantic models (`model.py`) | DONE | Same run. 15 model tests passed (facet_binding validator, justification-edge constant, defaults). Independently CONFIRMED_DONE. |
| T1.3 | Store: node create/read + commit DAG (`store.py`) | DONE | Run 20260711-215704-M1-kernel-store. 13 store-node tests pass; verifier CONFIRMED_DONE (re-ran pytest). Opus main-session audited store.py vs §4.5/§4.4/§4.1: create/commit/get_node(as_of)/history all correct, transactional, append-only. Logged SPEC-QUESTION T1.3 (object-blob layout, commit-hash addressing, history ordering — narrowest reading, open). |
| T1.4 | Store: edges create/retract + neighborhood/search | DONE | Run 20260711-215704-M1-kernel-store. 9 edge tests pass; verifier CONFIRMED_DONE. Opus audit vs §4.5/§4.2/§4.4: create_edge reuses Edge pydantic validator (no reinvented rule), retract is soft (retracted_at, never DELETE), neighborhood filters retracted_at IS NULL both directions, FTS sync wired into create_node (INSERT) + commit_node (UPDATE), search via nodes_fts MATCH. No spec questions. |
| T1.5 | Maturity derivation (`maturity.py`) | DONE | Run 20260711-215704-M1-kernel-store. 16 tests pass; verifier CONFIRMED_DONE. Opus audit vs §4.6: S0-S4 ladder correct, task/entity S2 facet-exemption handled, pure fn (no DB). Store-recompute wiring DEFERRED to T1.6 (store.py not in T1.5 Files). Logged SPEC-QUESTION T1.5 on S4-vs-S3 chaining ambiguity (literal reading: vetted alone => S4) — open, needs human. |
| T1.6 | Deletion, tombstone, redirects, split/merge | DONE | Run 20260711-215704-M1-kernel-store. 24 tests pass; verifier CONFIRMED_DONE. Opus audit vs §4.5/§4.6/§4.4: delete_node S0 hard-delete (node/commits/incident edges/fts) vs S1+ tombstone+redirect vs E_NEEDS_REDIRECT; split/merge insert redirects + reassign live inbound edges (zero dangling). Maturity wiring (deferred from T1.5) landed here: _recompute_maturity called in create_edge/retract_edge/commit_node/delete/split/merge, same-txn. commits.py minimal facets_touched + default_change_class helpers. 3 SPEC-QUESTIONs logged (split parts shape, merge survivor, facets_touched span rule) — open. |
| T1.7 | S0 garbage collection job | DONE | Run 20260711-215704-M1-kernel-store. 6 tests pass; verifier CONFIRMED_DONE. Opus audit vs §4.4/§4.5: gc_objects reachable = commits.object_hash ∪ nodes.head_hash ∪ sync_files.base_hash; deletes only orphans (all−reachable), single txn, returns sorted deleted hashes. Critical 'never collects live-S0 head' case covered. SPEC-QUESTION T1.7 logged (reachability widened past literal 'S1+ heads/history' to preserve 'never removes a referenced object') — open. |
| T1.8 | Store property suite | DONE | Run 20260711-215704-M1-kernel-store. Stateful hypothesis test passes (25 examples); verifier CONFIRMED_DONE. Opus audited test body (not vacuous): all 4 §4.5 invariants genuinely asserted — no-dangling-edges + head-reachable-to-genesis after EVERY op; as-of correctness + GC-safety per sequence, with independently-computed expected values. Scope note: delete/split/merge not in the random sequence (covered by T1.6 unit tests); the 4 enumerated invariants are fully exercised. |
| T1.9 | 10k-node neighborhood benchmark | DONE | Run 20260711-215704-M1-kernel-store. Passes (p95 < 50ms); verifier CONFIRMED_DONE. Opus audit: genuinely seeds 10,000 nodes + ~30k edges via store API, 500 timed neighborhood(hops=1) samples, real p95 via statistics.quantiles, asserts <0.050s (threshold not weakened). |

## M3 — Contract parser / renderer (Depends on: M2) — **CLOSED**

Milestone DoD: golden corpus ≥25 cases (29 actual: 18 canonicalization-only from
T2.4 + 11 new contract-grammar-focused cases covering tasks/nesting/embeds/refs/
`^tm-new`/all 5 violation codes/pause&diff); hypothesis `render(parse(D)) == D`
and `parse(render(G)) == G` both pass with no falsifying example (stress-tested
to 3000 examples/direction, T3.4 + T3.7's fuzz hunt); fuzz corpus committed
under `tests/golden/serialization/fuzz/` (honest empty-with-README — no
falsifying example was ever found to shrink). Verified locally: `uv run ruff
check src tests && uv run pyright src && uv run pytest tests/unit tests/property
tests/golden -q` → 249 passed, ruff/pyright clean. All T3.1–T3.7 independently
CONFIRMED_DONE by a separate verifier per task (re-ran every Verify command,
read-through against spec, adversarial/boundary probes on T3.5/T3.6). Run log:
`docs/agents/logs/20260711-M3-contract/`. From this milestone onward, edit
delegation followed the updated 3-tier fleet policy (Opus orchestrates, Sonnet
fleet-workers own each task and decide direct-vs-Cursor, Cursor Grok 4.5 High
executes well-specified/mechanical edits) — T3.3, T3.5, T3.6, T3.7 were
Cursor-delegated and Sonnet-verified; T3.1, T3.2, T3.4 were direct Sonnet edits
(T3.4 because hypothesis-strategy design was judgment-heavy). 4 open
SPEC-QUESTIONs logged this milestone (T3.1 `task_form` nonterminal, T3.2 `tm:`
version-mismatch handling, T3.5 E_DUP_ID scope, T3.6 pause-ratio denominator +
review-item code) — none deep-impact enough to block M4; all took the
narrowest, most conservative reading and are safe defaults to build on.

| Task | Goal | Status | Notes |
|---|---|---|---|
| T3.1 | Grammar tokens + contract version (`grammar.py`) | DONE | Run 20260711-M3-contract. 32 tests pass; ruff/pyright clean; full unit+property 164 passed. Independently CONFIRMED_DONE (re-ran tests, read both files, confirmed EOL-only anchor semantics, no parsing logic present, only listed files touched). SPEC-QUESTION T3.1 resolved 2026-07-12 (fable review) — narrowest reading (mirrors task_line's indent+checkbox prefix) CONFIRMED correct and final; see docs/archived-questions.md. |
| T3.2 | Parser: vault text → BlockSet (`parser.py`) | DONE | Run 20260711-M3-contract. 18 tests pass; ruff/pyright clean; full unit+property 182 passed. Independently CONFIRMED_DONE (regex reuse from grammar.py confirmed — no re-derivation; fence suppression, mid-line-anchor rejection, unmanaged->empty, parent-child-via-depth all read-through + test-confirmed). `BlockSet` (pydantic): `managed`, `contract_version`, `blocks: dict[id, Block]` (paragraph/task, `task_state`, `depth`, `parent_id`), `embeds`, `refs`, `new_requests` — this is the shape T3.3/T3.5 must consume. SPEC-QUESTION T3.2 resolved 2026-07-12 (fable review) — narrowest reading (exact-match-only is managed) CONFIRMED correct and final; see docs/archived-questions.md. |
| T3.3 | Renderer: hub nodes → canonical contract text (`render.py`) | DONE | Run 20260711-M3-contract, edited via Cursor (grok-4.5-high) + Sonnet verify. 8 tests pass; ruff/pyright clean; full unit+property 190→204 passed as sibling tasks landed. Independently CONFIRMED_DONE (reuse of parser.BlockSet/Block/Embed/Ref, grammar constants, kernel.ids.contract_anchor, kernel.canonical.canonicalize_text all read-through confirmed; manual idempotence probe reproduced). `render(block_set: BlockSet, *, resolve_body=None) -> str`, pure function, no DB dependency — reuses parser.BlockSet directly so parse/render stay symmetric for T3.4. |
| T3.4 | Round-trip property tests | DONE | Run 20260711-M3-contract, direct Sonnet edit (judgment-heavy hypothesis strategy design). 2 passed (render∘parse and parse∘render directions), stress-tested at max_examples=500 with no falsifying example. Independently CONFIRMED_DONE — verifier reproduced 3x with random seeds (no flakiness), confirmed real checksum-valid id generation via kernel.ids (not hand-rolled), confirmed genuine variety (0-8 items, all 4 block kinds, realistic nesting) and that scoping exclusions (new_requests, embed/ref line_no collisions) are documented/justified, not silent weakening. No falsifying examples found in parser.py/render.py — no latent bug. |
| T3.5 | Linter: violation codes + certain-repair (`linter.py`) | DONE | Run 20260711-M3-contract, edited via Cursor (grok-4.5-high) + Sonnet verify. 14 tests pass; ruff/pyright clean; full unit+property 204 passed. Independently CONFIRMED_DONE — verifier traced E_ID_CHECKSUM/E_DELETED_S1 code paths and confirmed NEITHER ever populates `repairs` (always `review_items`), confirmed E_DUP_ID/E_LOST_ANCHOR repairs require exact byte-identical match to base, and ran 4 live adversarial probes against `lint()` that all correctly avoided auto-repair. `lint(base: BlockSet, vault: BlockSet, vault_text: str, maturity=None) -> LintResult` (`.violations`, `.repairs`, `.review_items`), pure function, reuses kernel.ids checksum validation. SPEC-QUESTION T3.5 resolved 2026-07-12 (fable review, highest-stakes of the M3 batch) — per-file `lint()` signature CONFIRMED correct and final; kernel §4.1 does NOT cover cross-file dup detection (different invariant: node-id uniqueness vs. block-occurrence uniqueness); M5/T5.4 must add a cross-file dup check reading the node→path projection mapping it already needs for `hub_state_for(path)` (no new DB table). See docs/archived-questions.md and the M5 row below. |
| T3.6 | Pause & diff (formatter-storm guard) | DONE | Run 20260711-M3-contract, edited via Cursor (grok-4.5-high) + Sonnet verify. Extends linter.py additively (T3.5's code untouched). 10 tests pass + 14/14 T3.5 regression clean; ruff/pyright clean; full unit+property 216 passed. Independently CONFIRMED_DONE — verifier independently reproduced the exact-25%-boundary case via a standalone script (1/4, 2/8 -> no pause; 3/8 -> pause), confirming the strict `>` comparator matches spec's "> 25%" exactly (not `>=`). `PAUSE_THRESHOLD=0.25`; `pause_threshold(result, base) -> bool` (ratio = distinct violation ids / len(base.blocks), False if base.blocks empty); `pause_and_diff(result, base, base_text, vault_text) -> PauseDecision \| None` (`PauseDecision.snapshot: str`, `.review_item: ReviewItem` — exactly one, carrying a difflib.unified_diff). Zero I/O, same pure-function discipline as rest of linter.py. 2 SPEC-QUESTIONs resolved 2026-07-12 (fable review) — both narrowest readings CONFIRMED correct and final; binding guardrail for M5/T5.4/T5.7/T8.x: never branch on the borrowed `ViolationCode` to identify pause items, use the `PauseDecision` wrapper / sync-status pause state instead; persist as `review_queue.cause_kind='violation'` (no new cause_kind). See docs/archived-questions.md and the M5 row below. |
| T3.7 | Golden corpus (≥25) + committed fuzz corpus | DONE | Run 20260711-M3-contract, edited via Cursor (grok-4.5-high) + direct Sonnet fuzz-hunt/README. 29 total golden case dirs (18 original untouched + 11 new `contract_*`); 33 tests pass; M3 DoD command (golden+roundtrip) 35 passed; ruff/pyright clean. Independently CONFIRMED_DONE — verifier confirmed all 18 pre-existing dirs byte-identical, re-ran canonicalize_text live against 7 new cases (all MATCH), and read the new `test_contract_focused_cases_parse_and_lint_behavior` in full confirming it asserts the SPECIFIC ViolationCode per case (not just no-crash) for all 5 violation codes plus pause_threshold==True with a real diff. fuzz/ is an honest empty-with-README placeholder (3000 examples/direction hunted, none found) — no fabricated failure case. |

## M4 — Daemon + API + CLI core (Depends on: M1, M3) — **CLOSED**

Milestone DoD: `pytest tests/integration/test_api.py tests/integration/test_cli.py`
→ 68 passed after T4.10/T4.11; agent-token mutation lands in the review queue as
`cause_kind=proposal` (`test_agent_writes_become_proposals`); OpenAPI
snapshot-diff test green (`test_openapi_snapshot.py`). Verified locally on
ubuntu 2026-07-12: ruff clean, pyright --strict 0 errors, full
unit+property+integration **331 passed**. Run log:
`docs/agents/logs/20260712-M4-daemon-api/`. Fleet execution: Opus session
orchestrated; fleet-orchestrator computed each cohort (T4.5 solo →
{T4.6 ∥ T4.8} → {T4.7 ∥ T4.9}); fleet-workers owned each task and were
independently gate-verified by the Opus session (authoritative `make check`
re-run on the combined tree after each parallel cohort landed). T4.1–T4.4
were direct Opus edits earlier in the session. Rebrand invariant held
(OpenAPI title `tm-daemon API`, lock file `tm-daemon.lock` — no product name
in on-disk paths/formats). All 13 M4 SPEC-QUESTIONs were resolved against
the vision on 2026-07-12 (see `docs/archived-questions.md`);
T4.10/T4.11 applied the two contract-changing resolutions (durable sync
roots and mint-on-approval create proposals), and the full gate was rerun.
Windows single-instance-lock branch (`msvcrt`) is code-reviewed + pyright-clean
but not runtime-exercised on this Linux host (deferred to M9/T9.1 Windows
battery).

| Task | Goal | Status | Notes |
|---|---|---|---|
| T4.1 | Auth: token classes, secrets, rate limits (`auth.py`) | DONE | Run 20260712-M4-daemon-api, direct Sonnet edit (security-sensitive: hashing/rate-limit design). 12 tests pass; ruff/pyright clean; full unit+property 228 passed. Independently CONFIRMED_DONE — verifier traced constant-time `hmac.compare_digest` usage, confirmed zero SQL writes in module, revoked-checked-after-secret-match ordering, sound sliding-window rate limiter (hand-traced), and ran an adversarial script (valid/wrong-secret/revoked/500-rapid-unlimited-calls) all correct. Bearer format `"{token_id}.{raw_secret}"`; `authenticate(conn, bearer_value, *, now=None) -> AuthContext`; exception hierarchy under `AuthError` (Malformed/UnknownToken/InvalidSecret/Revoked/RateLimitExceeded, each with `.code`); `mint_secret()`/`hash_secret()`/`format_bearer_token()` exposed for T4.5/CLI to reuse when minting new tokens. Rate limiting in-process/in-memory sliding 60s window, resets on restart (documented, not a truth invariant). |
| T4.2 | Audit log | DONE | Run 20260712-M4-daemon-api, direct Opus edit. 16 tests pass; ruff/pyright clean; full unit+property 244 passed. Append-only audit primitive: `store.append_audit(conn, token_id, action, detail=None)` stamps `ts` via store `_now()` and issues one INSERT (rule 0.4 — the raw SQLite write lives in store.py, NOT auth.py). Policy layer in `api/auth.py`: `MUTATING_METHODS` (POST/PUT/PATCH/DELETE), `is_mutating_method()`, `record_mutation(conn, method, action, ctx, *, detail=None) -> bool` — writes exactly one row iff mutating (reads return False, write nothing), logs `ctx.token_id` (never the bearer secret; `AuthContext` structurally cannot carry it). This is the primitive T4.3's FastAPI middleware wraps per request. SPEC-QUESTION T4.2 logged (Files list omits store.py but non-negotiable rule 0.4 forces the audit INSERT into store.py; took that reading) — open, low-stakes confirmation. |
| T4.3 | FastAPI factory + `/health` + schemas re-export | DONE | Run 20260712-M4-daemon-api, direct Opus edit. 5 tests pass; ruff/pyright clean; full unit+property+test_health 249 passed. `create_app(config=None) -> FastAPI` factory (not a singleton — per-app for tests + T4.9 daemon); resolved `Config` stashed on `app.state.config` so serving layer binds `config.bind` (127.0.0.1 default). `GET /health` unauthenticated, returns `{status, version, contract_version}`; `app_version()` reads installed distribution metadata (never drifts from pyproject). `api/schemas.py` = thin verbatim re-exports of kernel/model.py (Node/Edge/Facet/…, `__all__`), zero divergence (spec §8 Phase-4 hook). Rebrand invariant honored: OpenAPI `title="tm-daemon API"` (no product name — it lands in the T4.7 snapshot). pyright false-positive on the decorator-registered `/health` closure suppressed with a targeted `reportUnusedFunction` ignore. SPEC-QUESTION T4.3 logged (`/health` path: `/v1` prefix vs literal `/health` — took literal per table cell + health-check convention) — open, low-stakes. |
| T4.4 | Node routes (`routes/nodes.py`) | DONE | Run 20260712-M4-daemon-api, direct Opus edit. 14 node tests pass (`-k nodes`); ruff/pyright clean; full unit+property+integration 264 passed. Endpoints under `/v1`: GET `/nodes/{id}` (+`?as_of=`, returns node+maturity), POST `/nodes` (201), PATCH `/nodes/{id}`, DELETE `/nodes/{id}` (S0 hard / S1+ 409 `E_NEEDS_REDIRECT`), GET `/nodes/{id}/history`·`/neighborhood?hops=`, POST `/nodes/{id}/split`·`/merge`, POST `/nodes/{id}/vet` (human-only ∅). **App plumbing added per approved defaults** (see AskUserQuestion 2026-07-12): `api/deps.py` (NEW) — `ApiError` + `register_error_handlers` rendering the spec §4.11 envelope `{"error":{code,message,detail}}` (incl. wrapping FastAPI `RequestValidationError`→422 `E_INVALID`), `get_conn`/`require_auth`/`require_human` dependencies; `require_auth` audits every authenticated mutating request once (reuses T4.2 `record_mutation`). `app.py` opens ONE shared WAL conn on `app.state.conn` (`check_same_thread=False`, first-run `mkdir` of tm-daemon dir), registers handlers, includes the router. `config.py` gained `db_path`/`default_db_path()` (→ `tm-daemon/store.db`, neutral per rule 0.6). `store.py` gained read-only `get_maturity` (Node model omits maturity) + `vet_node` (no vet fn existed — M1-close flagged this as the M4 vet-recompute-in-txn follow-up; sets vetted=1 + recomputes maturity in-txn, rule 0.4). `store.connect` gained `check_same_thread` kwarg (default True, back-compat). Agent-proposal rewriting NOT here (T4.6 cross-cutting dep); split reassignment queue NOT here (M7/T7.6) — both noted in code. T4.3's `test_health.py` updated to inject an in-memory conn (create_app now opens a DB). 2 SPEC-QUESTIONs logged (T4.4 db-path/shared-conn; T4.4 merge request shape) — open, low-stakes. |
| T4.5 | Edge, search, token, vault routes | DONE | Run 20260712-M4-daemon-api, fleet-worker (direct edit) + Opus-session independently verified (re-ran gate: ruff clean, pyright 0, T4.5 verify 18 passed, full unit+property+integration 282 passed; read all 20 new tests to confirm holistic coverage per user requirement). New `routes/{edges,search,tokens,vaults}.py`, all included in `app.py`. `POST/DELETE /edges` reuse T1.2's `Edge.facet_binding` validator via `store.create_edge` (missing binding on justification→400 `E_INVALID`; composes/redirects_to allow None); DELETE is a SOFT retract via `store.retract_edge` (test asserts `retracted_at` set, row survives, drops from neighborhood). `GET /search` wraps `store.search` (FTS5; tested real hit + empty non-match). `/tokens` and `/vaults` are FULLY human-only ∅ on every verb (GET included) per the literal §4.11 row — agent→403 `E_HUMAN_ONLY`. `store.py` gained `create_token`/`revoke_token`/`list_tokens` (+`TokenNotFoundError`, `_mint_unique_token_id` reusing the id8 scheme) — real persistent writes, never re-expose `secret_hash`, raw bearer returned exactly once at creation — and read-only `list_synced_vaults` (derives from `sync_files.vault`). store.py outside T4.5 Files but forced by rule 0.4 (same precedent as T4.2 `append_audit`). **`/vaults` scoping gap resolved WITHOUT new schema**: no `vaults` table exists in the frozen §4.4 DDL, and adding `migrations/002_vaults.sql` would break the existing acceptance test `tests/unit/kernel/test_schema.py::test_no_unlisted_tables_beyond_spec_and_bookkeeping` (rule 2 schema-freeze). So `POST /vaults` upserts a **process-local in-memory registry** (mirrors `auth.py`'s rate-limiter precedent) merged with `sync_files`-derived names on GET — **NOT durable across daemon restart**, flagged for M5/T5.1 to fix with its own schema task. 2 SPEC-QUESTIONs logged (store.py Files-list tension; `/vaults` schema gap) — both open. |
| T4.6 | Agent-token proposal rewriting | DONE | Run 20260712-M4-daemon-api, direct Sonnet edit. Named Verify `test_agent_writes_become_proposals` passes; full `tests/integration/test_api.py` 40 passed (8 new T4.6 tests); ruff/pyright clean; full gate `tests/unit tests/property tests/integration` 290 passed. New shared dependency `api/deps.py::mutation_gate(conn, ctx, request, *, node_id, payload=None) -> dict|None`: human tokens → `None` (route mutates as normal, unchanged); agent tokens → does NOT mutate, instead calls new `kernel/store.py::enqueue_review(conn, node_id, cause_kind, *, cause_ref=None, facet=None)` (rule 0.4 — the raw `review_queue` INSERT lives in store.py, same precedent as T4.2/T4.4/T4.5) with `cause_kind="proposal"` and `cause_ref` = canonical JSON (`kernel/canonical.canonical_json`, never pickle/eval) of `{method, path, body}`, returns the persisted review row. Wired into every non-∅ mutating route: `routes/nodes.py` (create/patch/delete/split/merge — all gated; only `/vet` stays exempt via existing `require_human`) and `routes/edges.py` (create/delete). Gated routes return **202** `{"proposed": true, "review": {...}}` for agents instead of their normal success status/body; `tokens.py`/`vaults.py` untouched (already fully `require_human`, confirmed still 403 `E_HUMAN_ONLY` for agents, never proposalized — asserted in new test `test_agent_rejected_outright_on_every_empty_endpoint_no_proposal`). New store.py helpers: `mint_unassigned_node_id` (reuses the existing node-id mint+collision-check scheme, zero new id format, for the `POST /nodes` create-proposal's `node_id` placeholder — NOT inserted as a real node) and `get_edge_dst` (read-only, used to pick `DELETE /edges/{id}`'s proposal target). 3 new SPEC-QUESTIONs logged (create-proposal `node_id` placeholder; edge-proposal `node_id`=`dst` choice; store.py Files-list touch, same recurring rule-0.4 tension as T4.2/T4.4/T4.5) — all open, low-stakes. Test cluster in `tests/integration/test_api.py` (8 new tests): named Verify test + human-mutates-directly + agent PATCH/DELETE /nodes + agent POST/DELETE /edges + agent split/merge + the ∅-endpoints-reject-not-proposalize test, each asserting both the HTTP response shape and the underlying DB state (no mutation happened, `review_queue` row count/fields, `cause_ref` round-trips via `json.loads`). |
| T4.7 | OpenAPI snapshot + CI gate | DONE | Run 20260712-M4-daemon-api, fleet-worker (direct Sonnet edit — mechanical but the "prove the gate isn't vacuous" requirement needed judgment, so not Cursor-delegated). Verify `tests/integration/test_openapi_snapshot.py` 3 passed; ruff/pyright clean on all 3 owned files; full gate `tests/unit tests/property tests/integration` 319 passed. New `docs/api-snapshot/openapi.json` (NEW file, `create_app(conn=:memory:).openapi()` serialized as canonical JSON — `sort_keys=True, indent=2, separators=(",", ": ")` + one trailing newline — chose an indented, git-diffable form over `kernel/canonical.canonical_json`'s compact hashing form since this is a human-reviewed migration-contract artifact, not a hash target); confirmed path set is exactly the currently-served routes (`/health`, `/v1/nodes*`, `/v1/edges*`, `/v1/search`, `/v1/tokens*`, `/v1/vaults`) — no `/review`, `/sync`, `/metrics` stubs added. `tests/integration/test_openapi_snapshot.py` (NEW) is both the test AND the regenerate tool (same `_canonical_snapshot_text()` helper drives both, so they can't drift apart): `test_served_spec_equals_committed_snapshot` (byte-equality), `test_gate_actually_catches_drift` (mutates a deep copy of the served spec with a fake extra path, asserts it does NOT match the snapshot — proves the comparison isn't vacuous), `test_snapshot_carries_no_product_name` (rebrand invariant, rule 0.6). Regenerate command (documented in the test file's module docstring): `uv run python -m tests.integration.test_openapi_snapshot`. `.github/workflows/ci.yml` extended minimally in existing style: added `- run: uv run pytest tests/integration/test_openapi_snapshot.py` as a step in the existing `check` job (right after the `tests/unit tests/property` step) and removed the now-obsolete "Reserved... openapi-snapshot" comment line; did not touch the `windows-gate`/battery/soak placeholders. **Verified the gate is real, not just self-consistent**: temporarily added a genuine extra route to `app.py` (`GET /__drift_probe__`), reran the named Verify test and confirmed `test_served_spec_equals_committed_snapshot` actually FAILS with a real diff, then reverted `app.py` byte-for-byte (diffed against a pre-edit backup) and reconfirmed all 3 tests pass again — `app.py` itself is untouched/unmodified in the final diff (confirmed via `diff` against backup, zero bytes changed). Confirmed no product-name leakage in the snapshot (`grep -i akasha docs/api-snapshot/openapi.json` → no matches; title is `tm-daemon API`). No new SPEC-QUESTIONs. Full-gate `pyright src` showed 9 transient errors, all in `src/akasha/daemon.py` (msvcrt Windows-lock code, `reportDeprecated` on `contextmanager`) — confirmed via `git diff --stat`/`git log` this is the concurrently-running T4.9 sibling task's in-progress, uncommitted edit to a file T4.7 does not own and never touched; `pyright src/akasha/api src/akasha/kernel src/akasha/contract` (T4.7's actual surface) is 0 errors/0 warnings on its own. |
| T4.8 | CLI verbs (`cli/main.py`, typer) | DONE | Run 20260712-M4-daemon-api, fleet-worker (direct Sonnet edit). Verify `tests/integration/test_cli.py` 26 passed; ruff/pyright clean; full gate `tests/unit tests/property tests/integration` 316 passed. New `src/akasha/cli/main.py`: a pure `typer` HTTP client (never imports `kernel/store.py`, never touches SQLite — only exception is `kernel.ids.mint()` for client-side facet-id generation, documented as pure/DB-free, same precedent as `contract/render.py`/`contract/linter.py` reusing it). Verbs `new/get/set/rm/search/review/token` wired to their §4.11 endpoints exactly (`akasha daemon` deliberately NOT here — T4.9's verb). Global flags `--json` (`cli/v1` envelope `{"schema","ok","data"|"error"}`, additive-only), `--dry-run` (mutating verbs print `{"method","path","body"}` and `typer.Exit(0)` **before** any `httpx` call — proven in tests by pointing `--dry-run` at an unreachable base-url and asserting no exception), `--token`; plus one CLI-only wiring flag `--base-url` (spec-silent, needed to point at a test daemon, defaults to spec's `127.0.0.1:7433`). Exit codes: `_exit_code_for` maps 404/`E_NOT_FOUND`→3, 409/`E_NEEDS_REDIRECT`/conflict-ish codes→4, everything else the server returns→1; genuine CLI-side usage errors (missing required args — handled automatically by click/typer; malformed `--facet`; bad `--class` value) →2 (verified for all four paths + a connection/auth-failure→1 case). `--facet name=span` (repeatable) mints a fresh `facet_id` client-side + `version=1`, matching the API's `Facet` model shape. Tests: real `uvicorn` server on an ephemeral 127.0.0.1 port (thread, `uvicorn.Server.run`), invoked via `typer.testing.CliRunner` against the actual `akasha.cli.main:app` entrypoint (true end-to-end HTTP, no ASGI shortcut) — 26 tests covering every verb's happy-path round-trip (new+get, set, rm S0, search, token create/list/revoke), `--dry-run` mutates nothing (asserted via DB row-count AND an unreachable-host proof), `--json` emits `cli/v1` on both success and error, and every exit-code path (get/rm/set/token-revoke missing→3; rm S1-without-redirect→4, S1-with-redirect→0; malformed `--facet`/bad `--class`/missing required args→2; missing token→1). Tokens seeded directly into the fixture's SQLite conn (same pattern as `test_api.py::_insert_token`) — every mutating round-trip uses a **human** token, independent of T4.6 (agent-token proposal rewriting, landed concurrently mid-task; re-ran the full gate afterward, still 316 passed). 3 new SPEC-QUESTIONs logged (`/v1/review*` doesn't exist until T7.5 — CLI implements the verb against the documented shape and tolerates FastAPI's non-envelope 404, tested via `test_review_list_against_unimplemented_endpoint_fails_gracefully` which asserts only "no crash, non-zero exit", not a round-trip; `set`'s `--class` default when omitted, took `patch`; `--base-url` as spec-silent CLI wiring) — all open, low-stakes. |
| T4.9 | Daemon lifecycle + single-instance lock + autostart docs | DONE | Run 20260712-M4-daemon-api, fleet-worker (direct Sonnet edit — judgment-heavy: cross-platform lock design, exit-code mapping). Verify `tests/integration/test_daemon_lock.py` 8 passed; ruff/pyright clean; full gate `tests/unit tests/property tests/integration` 327 passed (incl. sibling T4.7's `test_openapi_snapshot.py`, unaffected — no new/changed FastAPI route added). Extends `daemon.py` (T0.6's `configure_logging`/`JsonLineFormatter` untouched, same signature) with: `AlreadyRunningError(lock_path)` (typed, human-readable `"another akasha daemon instance is already running (lock held at {path})"`); `single_instance_lock(lock_path)` context manager — non-blocking exclusive OS lock, `fcntl.flock(LOCK_EX\|LOCK_NB)` on POSIX / `msvcrt.locking(LK_NBLCK, 1)` on Windows, gated on `sys.platform` (each Windows-only helper starts with an `if sys.platform != "win32": raise AssertionError(...)` guard so pyright's platform-aware reachability analysis skips checking the Windows-only stdlib calls on the Linux dev/CI host — 0 pyright errors without weakening real Windows behavior); releases on any exit (normal or exceptional), verified by both a clean-exit and an exception-exit test. Lock file: `tm-daemon.lock` in the config directory (`Config.path.parent`, falling back to `config.default_config_dir()` — neutral name, rule 0.6, no product name on disk). `serve(config)` ties it together: resolves the config dir, calls `configure_logging(dir/"daemon.log")`, acquires the lock, then lazily imports `akasha.api.app.create_app` + `uvicorn.run(host=config.bind, port=config.port)` — both imports deferred into the function body so `cli/main.py`'s top-level import stays cheap and `daemon.py` doesn't force FastAPI/uvicorn onto every CLI invocation. `cli/main.py` gained the `daemon [--config PATH]` verb (module docstring updated to explain why this one verb breaks the "pure HTTP client" pattern — it doesn't speak HTTP to a running server, it *is* the server, dispatching to `daemon.serve`); it does NOT use `--base-url`/`--token`/`--json`/`--dry-run` (foreground process command, not an API call) — catches `AlreadyRunningError` and exits **4** (the spec §4.12 "conflict" class: a second instance is a conflict over the single-instance-lock resource, not error(1)/usage(2)), printing a one-line message, never a traceback (asserted in a test that pre-holds the lock then invokes the CLI verb via `CliRunner`). `docs/autostart-windows.md` (new): Task Scheduler XML sample (`LogonTrigger`, `MultipleInstancesPolicy=IgnoreNew`, `ExecutionTimeLimit=PT0S` disabling the 72h default kill, `RestartOnFailure`) + `schtasks` one-liner alternative, and NSSM install/configure/manage steps including the `LocalSystem`-vs-per-user `%APPDATA%` config-path gotcha. No FastAPI route added/changed (daemon is CLI/process-only, confirmed safe alongside T4.7's parallel OpenAPI-snapshot work). No new SPEC-QUESTION — build-plan Steps + M4 milestone text fully specified the lock/docs requirement; the exit-code-4 choice and lock-file-location choice were narrowest-reading judgment calls documented inline, not spec gaps. |
| T4.10 | Durable sync-root registry + terminology refinement | DONE | Added forward migration `002_refine_m4_contract.sql`; `/v1/sync/roots` persists registrations through `kernel/store.py`, survives reconnect/restart before any file sync, and replaces the removed `/v1/vaults` route. `sync_files` now references `sync_roots`. OpenAPI snapshot regenerated intentionally. |
| T4.11 | Create-proposal identity refinement | DONE | `review_queue.node_id` is nullable; agent `POST /nodes` stores canonical `{method,path,body}` with `node_id=NULL`, creates no node, and reserves no id. Existing-node and edge proposals retain their affected-node subject. T7.5 now owns minting on human approval. |

## M5 — Sync engine (Depends on: M4, T4.10) — **CLOSED**

Milestone DoD (spec): `make battery` — the scripted edit battery §6.2 passes 100%
with 0 silent guesses; `test_crash_recovery_idempotent` converges on restart.
Verified locally 2026-07-13: `uv run pytest tests/battery` → **27 passed** (E01–E20
+ E04b S0-cross-file-move, incl. the silent-guess counter == 0);
`test_crash_recovery_idempotent` → 1 passed; full `uv run ruff check src tests &&
uv run pyright src && uv run pytest tests/unit tests/property tests/integration`
→ ruff clean, pyright --strict 0 errors, **437 passed**; zero `tests/golden/**`
modifications. Fleet execution: Opus session orchestrated; fable model designed
the deep tasks (T5.4 reconcile, T5.5 conflict-branch, T5.8's 3 fixes) and the
product owner ratified the 5 T5.4 gaps + the T5.8-2 losslessness decision;
fleet-workers implemented, each independently gate-verified by the Opus session.
**The T5.8 edit battery did its job — it surfaced 3 real sync-engine bugs, all
fixed before close:** T5.8-1 (E20 O(n²) LCS → O(n log n) LIS + fast-path, 15s→1.2s),
T5.8-2 (write-back losslessness — managed files are now lossless containers,
additive `BlockSet.raw_lines`/`front_matter`, §4.7 spec sentence added), T5.8-3
(cross-file S0 move no longer lossy — withhold delete when the anchor is provably
in another managed file under the same root). Open follow-ups (spec-questions,
non-blocking): T5.5 fork-point provenance / as-of-across-branches / deleted-op
asymmetry; T5.8-4 (hub-side S0 hard-delete leaves an orphan vault line, surfaced
as E_UNKNOWN_ANCHOR — would need an S0-delete-tombstone kernel change). Next
milestone **M6 (Obsidian plugin)** and **M7 (TMS loop)** both unblock on M5.

**Follow-ups inherited from M3's resolved spec questions (fable review, 2026-07-12
— see docs/archived-questions.md T3.5 and T3.6 entries for full reasoning):**
1. **T5.4** must add cross-file `E_DUP_ID` detection (reconcile-level, reading the
   node→path projection mapping already required for `hub_state_for(path)`) and
   must distinguish E04 (cross-file move) from cross-file E05 (copy-without-cut)
   by correlating `created`/`deleted` ops across files within the reconcile
   horizon — anything uncertain ⇒ review item, never a guess.
2. **T5.4/T5.7/T8.x** must persist pause-and-diff review items as
   `review_queue.cause_kind='violation'` (no new `cause_kind`) and must never
   branch on `linter.PauseDecision.review_item.code` (a cosmetic borrowed value)
   to identify a pause event — use the `PauseDecision` wrapper at the reconcile
   boundary and `/sync/status` pause state at the API/UI boundary instead.

| Task | Goal | Status | Notes |
|---|---|---|---|
| T5.1 | Base store (per-file snapshots) | DONE | Fleet-worker (direct Sonnet edit). Verify `tests/unit/sync/test_base_store.py` 11 passed; ruff/pyright clean; full gate `tests/unit tests/property tests/integration` 342 passed. `src/akasha/sync/base_store.py` (NEW): thin `put(conn, sync_root_id, path, data: str) -> None` / `get(conn, sync_root_id, path) -> str \| None` — canonicalizes via `kernel.canonical.canonicalize_text`, delegates every SQLite read/write to `kernel/store.py` (rule 0.4). Public API is `str` in/out (never raw `bytes`); UTF-8 only exists at the `objects.bytes` BLOB boundary inside `store.py`. `store.py` gained (rule 0.4, outside T5.1's Files list, same precedent as T4.2/T4.4/T4.5/T4.6): `SyncRootNotFoundError`, `sync_root_exists(conn, sync_root_id) -> bool`, `_insert_base_snapshot(conn, canonical_text, now) -> str` (content-addressed `objects` insert, `kind="base_snapshot"`, hash = `object_hash` of the **raw** canonical UTF-8 bytes — NOT canonical-JSON-wrapped like node snapshots), `write_base_snapshot(conn, sync_root_id, path, canonical_text) -> str` (validates the sync root raises `SyncRootNotFoundError` before opening any transaction, then one transaction: insert object + `sync_files` upsert keyed by `path`, preserving existing `contract_version` or defaulting to `1`), `read_base_snapshot(conn, sync_root_id, path) -> str \| None` (scoped: wrong root or no row/`base_hash` ⇒ `None`). Confirmed `gc_objects` (T1.7) still treats new `base_snapshot` objects as reachable via `sync_files.base_hash` purely by hash lookup — added a regression test. `get()` on an unknown `sync_root_id` reads as `None` (not an error) — validation is a `put()`-only concern per the build-plan Steps wording; `put()` on an unknown root raises `SyncRootNotFoundError` and persists nothing. `tests/unit/sync/test_base_store.py` (NEW, 11 tests, no `__init__.py` needed — sibling dirs don't use one): put/get round-trip, sync-root scoping (wrong-root read misses, DB row asserted), fresh path → `None`, unknown-root rejected on `put` (no `sync_files` row written) / reads `None` on `get`, CRLF/non-NFC/missing-trailing-newline all canonicalized not stored verbatim, idempotent re-`put` of identical bytes reuses the same `objects` row (hash + row-count asserted equal), re-`put` with different content advances `base_hash` while the old object row survives (append-only), GC-reachability regression. 3 SPEC-QUESTIONs logged (store.py Files-list touch; base-snapshot `objects` content shape = raw bytes not JSON-wrapped; `sync_files.contract_version` default-to-1 on first `put`, flagged for T5.4 to possibly parameterize) — all open, low-stakes. |
| T5.2 | Origin / echo-suppression (`origin.py`) | DONE | Fleet-worker (direct Sonnet edit — small, judgment-heavy bound-policy choice, not Cursor-delegated). Verify `tests/unit/sync/test_origin.py` 8 passed; ruff/pyright clean; full gate `tests/unit tests/property tests/integration` 350 passed. `src/akasha/sync/origin.py` (NEW): process-local, in-memory only — no SQLite, no `store.py` touch (same precedent as `api/auth.py`'s in-process rate-limit call log: transient operational state, not a truth invariant, resets on daemon restart). `class OriginTracker(*, max_pending: int = 256, ttl_seconds: float = 30.0)` with `record_write(path: str, hash: str, *, now: float \| None = None) -> None` and `is_echo(path: str, hash: str, *, now: float \| None = None) -> bool`. Match requires BOTH `path` and `hash` equal; a hit **consumes** the record (removed from the pending queue) so the same `(path, hash)` suppresses exactly once — an immediate repeat is treated as a genuine external write. `now` is an injectable monotonic-clock hook (mirrors `auth.py::check_rate_limit(..., now=...)`) so tests are deterministic; defaults to `time.monotonic()`. Internal state: a single `deque[tuple[path, hash, recorded_at]]` guarded by one `threading.Lock` (watcher's observer thread calls `is_echo` while the daemon's own thread calls `record_write`). **Bound policy** (documented in the module docstring): dual bound — count (`max_pending=256`, oldest evicted once exceeded; covers a large multi-file reconcile burst per §6.2's battery without unbounded growth) AND age (`ttl_seconds=30.0`, evicted lazily on the next call; comfortably longer than the §4.8 500ms debounce window plus OS/cloud-sync FS-event latency, so a genuine echo is never missed while a write that never echoes doesn't leak memory forever). `tests/unit/sync/test_origin.py` (NEW, 8 tests, no `__init__.py` per T5.1's sibling-dir precedent): exact-match suppressed exactly once (consumed); different-hash-same-path not suppressed (and original stays pending); same-hash-different-path not suppressed (and original stays pending); no-prior-write not suppressed; multiple distinct pending writes each independently suppressed in a different order than recorded; count-bound eviction (oldest dropped past `max_pending=2`); TTL-bound eviction (dropped once injected clock advances past `ttl_seconds`, still-pending just under the TTL); TTL eviction of one record doesn't disturb a still-fresh sibling record. No SPEC-QUESTION — spec §4.8's echo-suppression sentence and the task's own Design section (bound-by-count/TTL, injectable clock, `threading.Lock`) fully constrained the implementation; the specific bound values (256 / 30.0) are a narrowest-reading judgment call documented inline, not a spec gap. |
| T5.3 | Watcher: debounce + cloud-path detection (`watcher.py`) | DONE | Run 20260712-M5, draft `watcher.py` (~17KB) written by a session-limited fleet-worker, then **critically reviewed + test-covered + verified by the Opus session** (the draft was NOT trusted blindly — 19 holistic tests were written from the E18/E19 DoD/spec, not reverse-engineered from the draft, and they validate it). Verify `tests/unit/sync/test_watcher.py` 19 passed; ruff/pyright clean; full unit+property+integration **369 passed**. Three-layer design (all independently testable): `detect_cloud_path(path) -> "OneDrive"|"Dropbox"|None` (pure, segment-level case-insensitive marker match); `Debouncer(on_cycle, *, debounce_seconds=0.5, now=None)` — poll-based trailing-edge debounce with injectable `at=` clock (no threads/sleeps), `notify(path, at=)` / `poll(at=) -> list[str]`, resets after fire (not one-shot); `Watcher(conn, on_cycle, *, debounce_seconds=0.5, now=None, origin_tracker=None, content_hash_fn=None, observer_factory=<real watchdog Observer>, logger=<akasha>)`. **T5.4 wiring contract:** `on_cycle: Callable[[str], None]` is the reconcile-routing seam (called with one path once its 500ms window elapses; nothing imports `sync.reconcile`). `load_roots()` reads `store.list_sync_roots` (T4.10), runs cloud detection per root → sets `WatchedRoot.conservative`/`.cloud_provider` + logs ONE warning via the `akasha` logger (E19). `start()`/`stop()` use an injectable `observer_factory` (real `watchdog.observers.Observer` default; test spy in tests). Optional echo-suppression: when `origin_tracker` (T5.2) + `content_hash_fn` both supplied, `notify_event` drops the daemon's own write echoes before debouncing. `watchdog` added to `pyproject.toml` (spec §4.8 names watchdog). `conservative` is runtime-only state (no schema/migration). No open SPEC-QUESTIONs (draft's segment-substring cloud match is a documented narrowest reading). |
| T5.4 | Reconcile pipeline (`reconcile.py`) | DONE | Run 20260712-M5, fleet-worker implementing the **fable architecture-review design** (7 resolutions + 5 human-decided spec gaps, aligned with fable 2026-07-12), independently gate-verified by the Opus session (ruff clean, pyright 0, T5.4 verify 31 passed, full unit+property+integration **400 passed**; confirmed reconcile.py has ZERO direct SQLite writes — all mutation via store API, DoD "no op outside store API" holds). `src/akasha/sync/reconcile.py` (~700 lines) implements §4.8 verbatim: quiet/hub-only shortcuts; `diff_blocks` calls `linter.lint()`, applies certain-repairs, computes 6 op kinds (modified/created/deleted/moved[LCS]/checkbox_toggled/reparented) on the repaired view with the **anchor∈ops XOR review_items partition**; `hub_state_for` (skeleton from base B / vault V_final, bodies from hub heads, no new table); `hub_changed_since` (content compare + convergent-edit no-op); `kernel_apply` (author="sync"); pause via `linter.pause_and_diff` persisted as `cause_kind="violation"` (never reads `.code`); in-memory rebuildable `ProjectionIndex` for cross-file E04-move vs E05-`E_DUP_ID`. **Conflict seam for T5.5:** `Reconciler(conn, origin, *, conflict_handler=<default>, projection=None)` — default enqueues `cause_kind="conflict"` + embeds vault version in `cause_ref` + skips (hub wins file, zero data loss); T5.5 swaps the handler WITHOUT editing the pipeline. `.on_change(path)` matches `Watcher(on_cycle=...)` for T5.6. **New store.py helpers (rule 0.4, reuse these):** `commit_node(..., task_state=<sentinel>)`, `list_sync_files`, `find_live_edges(src=,dst=,edge_type=)`, `write_base_snapshot(..., contract_version=None)` — the last CLOSES T5.1's contract_version SPEC-QUESTION. **Real bug caught+fixed:** canonical trailing-`\n` mismatch between `node.body` and `Block.text` (would false-conflict every edit) — fixed with a `_body_line` rstrip helper at every hub-body comparison. 2 minimal documented extensions: `Op.parent_id` (for minted-task composes edge), `DiffOutcome.extra_review_items` (cross-file findings can't use linter's frozen `ReviewItem.code`). 6 golden cases seeded under `tests/golden/reconcile/{modified,checkbox,create-tm-new,delete-s0,move,conflict}/` (generated by running the pipeline; T5.8 owns the full E01-E20 battery + must not edit these 6). 5 decided design choices documented inline as `# design note (T5.4, fable-reviewed, human-decided 2026-07-12)`: PARAGRAPH_NODE_TYPE="claim", SYNC_CHANGE_CLASS="patch" (M7/T7.2 seam), multi-line-body→violation+keep-base, E_DELETED_S1→no-resurrect, pause-snapshot-in-cause_ref. No new open SPEC-QUESTIONs. |
| T5.5 | Conflict branching | DONE | Run 20260713-M5, fleet-worker implementing the **fable architecture-review design**, independently gate-verified by the Opus session (ruff clean, pyright 0, T5.5 verify 1 passed, kernel/store regression 99 passed, full **406 passed**). Replaces T5.4's minimal conflict seam with real commit-DAG branching: on a both-sides edit, the vault version is recorded as a commit on the node's DAG parented on the current head commit, **without moving `nodes.head_hash`** (hub head stays the file winner → zero data loss), and exactly one `cause_kind='conflict'` review is enqueued. New store.py helpers (rule 0.4, reuse these): `record_conflict_branch(conn, node_id, branch_body, *, task_state=<sentinel>, author="sync", message="") -> str` (content-addressed + `(node_id,object_hash)` idempotence gate; never touches head/maturity), `_head_commit_hash`, **`find_open_reviews(conn, *, node_id=None, cause_kind=None, cause_ref=None) -> list[dict]`** (general open-review reader — **T5.7's `/sync/status` reuses this verbatim**), `get_commit_snapshot(conn, commit_hash) -> {body,facets,task_state}`. **CRITICAL companion fix:** `commit_node`'s parent lookup now resolves via `_head_commit_hash` (not newest-by-rowid) so a later mainline commit never silently collapses onto a branch — verified bit-identical (all 99 kernel/store + property tests green; integration test pins it: post-branch `commit_node` parents on the head commit, not the branch). `kernel/commits.py` gained pure `conflict_branch_message`/`conflict_cause_ref`. `reconcile.py`: `conflict_branch_handler` swapped in as the `Reconciler.conflict_handler` default (no pipeline edit — the T5.4 seam sufficed); `deleted`-op conflict enqueues a review with `branch_commit=null` and no branch (nothing to preserve vault-side). **Idempotence verified end-to-end** (crash-replay: same content-addressed object + deterministic `cause_ref` → no dup commit, no dup review) — de-risks T5.6 startup re-reconcile. Tests: `tests/integration/test_conflict.py` (E12) + `tests/unit/kernel/test_conflict_branch.py` (5 isolated: head untouched, double-call dedup, sentinel, unknown-node, GC keeps branch object). 3 SPEC-QUESTIONs logged (fork-point provenance; as-of-vs-branches [minor]; deleted-op-conflict asymmetry [informational]) — all open. |
| T5.6 | Startup reconcile / crash recovery | DONE | Run 20260713-M5, fleet-worker, independently gate-verified by the Opus session (ruff clean, pyright 0, E11 DoD `test_crash_recovery_idempotent` passed, crash_recovery 5 passed, full **416 passed**). `reconcile.py` gained `reconcile_all(conn, origin=None, *, projection=None) -> {files_reconciled, files_missing, reviews_open}` (pure addition — constructs one `Reconciler` and drives `.on_change` over every `store.list_sync_files` path, catching per-file `FileNotFoundError` as `files_missing`; this is the reusable startup/rescan core, T5.8 can reuse it). `daemon.serve()` wires it in: after `create_app(config)`, INSIDE `single_instance_lock` (so two daemons never reconcile concurrently), before `uvicorn.run` — lazy imports matching T4.9 style. Because §4.8 `on_change` is idempotent (content-addressed objects + quiet/hub-only shortcuts from T5.4, conflict dedup from T5.5), startup reconcile IS crash recovery. E11 test simulates the worst-case crash point (hub `commit_node` already applied "line two" but daemon died before write-back/`base_store.put` → stale base + raw vault file on restart): `reconcile_all` converges (file==hub render==base_store, anchor never lost, `_vault_matches_hub` recognizes convergent no-op so NO duplicate commit), and a 2nd `reconcile_all` makes zero further writes (file bytes/base/commit-history/review-count all unchanged). 5 tests: idempotent (E11), edits-while-down, empty-noop, missing-file-skip, and startup-runs-inside-lock (monkeypatched uvicorn). No new SPEC-QUESTION. |
| T5.7 | Sync API routes (`routes/sync.py`) | DONE | Run 20260713-M5, fleet-worker, independently gate-verified by the Opus session (ruff clean, pyright 0, `-k sync` 9 passed, openapi snapshot 3 passed [drift test still non-vacuous], full `test_api.py` 47 passed [pre-existing tests intact], full **411 passed**). New `routes/sync.py` (included in app.py): `GET /v1/sync/status` (read-only — `list_sync_roots`+`list_sync_files`+**reuses T5.5's `store.find_open_reviews`** for violations/pauses/conflicts; a pause = open `cause_kind='violation'` review whose `cause_ref.pause==true`, split by parsing cause_ref JSON NOT by branching on any borrowed ViolationCode — M5 follow-up 2 honored; reviews correlated to roots via `cause_ref.path`, unresolved ones bucketed not dropped) and `POST /v1/sync/rescan` (constructs `Reconciler(conn, OriginTracker())` per request, runs `on_change` for every `list_sync_files` path through the pipeline/store API — no direct SQLite write from the route; returns `{files_reconciled, files_missing, reviews_open}`; a vanished file is caught as `files_missing`, not a 500). Both `require_auth` (not ∅ per §4.11), and `/sync/rescan` deliberately NOT `mutation_gate`-proposalized (operational trigger, no authored body; converges as author="sync" regardless of caller — documented inline). `store.list_sync_files` widened to also return the existing `last_synced_at` column (read-only, additive). OpenAPI snapshot regenerated in-change via `python -m tests.integration.test_openapi_snapshot`; no product-name leak. Response shapes: status `{sync_roots:[{id,name,root_path,files,violations,pauses,conflicts}],unresolved}`, rescan `{files_reconciled,files_missing,reviews_open}`. No new SPEC-QUESTION. |
| T5.8 | Golden reconcile fixtures + scripted edit battery E01–E20 | DONE | Run 20260713-M5. `tests/battery/test_edit_battery.py` + golden fixture dirs under `tests/golden/reconcile/` (E01/02/03/06/08/12 = T5.4's 6 seeded dirs reused verbatim under their E-numbers; E07/09/10/17 new 5-file goldens; E04/05/16 two-file goldens; E04b new S0-cross-file-move; E11/18/19/20 behavioral). Silent-guess counter: for the review/pause/ignore cases (E05,E07,E13,E14,E15) snapshots `store.history()`/body before vs after `on_change`, asserts zero kernel mutation, summed == 0. **Battery caught 3 real sync-engine bugs; the worker correctly refused to weaken tests or edit src (out of its tests-only Files list); all 3 fixed via fable-designed follow-ups + product-owner decision, then independently gate-verified by the Opus session:** (T5.8-1) E20 O(n²) `_lcs_ids` → `_stable_order_ids` fast-path + O(n log n) patience-LIS with a Hypothesis oracle test proving DP-equivalence; **E20 15s→1.2s**. (T5.8-2) write-back losslessness → managed files are now **lossless containers**: additive `BlockSet.raw_lines`/`front_matter`, `render` interleaves raw lines by position, `hub_state_for` preserves missing-node blocks; round-trip property extended (prose/blank/fence, 500 examples) holds both directions; **zero golden bytes changed**; §4.7 spec sentence added. (T5.8-3) cross-file S0 move → `_compute_ops` gains an injected `anchor_elsewhere` scanner (`Reconciler` walks the sync root, substring-prefilter + parse-confirm) that withholds the delete only when the anchor is provably a live managed block in another file; E06 hard-delete still fires (nothing elsewhere). **Final: `tests/battery` 27 passed (100%, silent-guess==0), `test_crash_recovery_idempotent` passed, full gate 437 passed, ruff/pyright clean, zero `tests/golden/**` modifications.** SPEC-QUESTIONs T5.8-1/2/3 RESOLVED; T5.8-4 logged open (hub-side S0 hard-delete orphan-line trade-off). **M5 CLOSED.** |

## M6 — Obsidian plugin (Depends on: M5) — **CODE-COMPLETE** (runtime DoD pending)

> All five tasks are code-complete and locally gated (esbuild build + `tsc
> --noEmit` clean; two-file scope per task; rebrand-clean; independently
> re-verified). Unlike M1–M5 (which closed on runnable Python suites), the
> M6 milestone DoD — "plugin build green **in CI**" + "`TESTPLAN.md` executed
> against a demo vault" — is **not** verifiable in this headless environment
> and remains **pending**: (a) CI plugin-build turns green on the first push
> (convention set by T0.8); (b) a human runs `plugin-obsidian/TESTPLAN.md`
> against a live Obsidian vault + daemon. Notably, T6.5's E05 (`E_DUP_ID` on
> copy-paste) rests on Obsidian retaining a duplicated `^tm-<id8>` verbatim
> on paste (very likely — Obsidian treats `^ids` as literal body text and
> does not dedupe; see the TESTPLAN's load-bearing-dependency note); if the
> manual run shows stripping, T6.5 reopens and needs `main.ts` wiring.

| Task | Goal | Status | Notes |
|---|---|---|---|
| T6.1 | Plugin scaffold + build in CI | DONE | Cursor-built TS scaffold (id `tm-hub`, rebrand-clean); `npm ci && npm run build` → `main.js` (verify exit 0, re-run independently); ci.yml `plugin-build` job (setup-node@v4/node20, working-dir plugin-obsidian, mirrors local). CI-green pending first push. |
| T6.2 | Settings (URL + token) | DONE | Cursor-built `settings.ts` (TmHubSettings/DEFAULT_SETTINGS url=127.0.0.1:7433, TmHubSettingTab: URL + password token) + `main.ts` loadData/saveData persistence, addSettingTab, `apiFetch` w/ Bearer. Local gate: build exit 0 + `tsc --noEmit` exit 0 (persist-across-reload is manual, deferred to T6.5 TESTPLAN). |
| T6.3 | Status bar (sync state + violation count) | DONE | Cursor-built `statusbar.ts` polls `GET /v1/sync/status` via apiFetch; violation count = Σ sync_roots[].violations + unresolved[bucket=violations]; renders synced/N-violations/offline; degrades on non-ok + thrown (no unhandled rejection). `main.ts` addStatusBarItem + registerInterval(5s). Gate: build + tsc exit 0. |
| T6.4 | Command: create node from selection | DONE | Cursor-built `commands.ts` `createNodeFromSelection(editor)` appends ` ^tm-new` at end-of-block (selection-end/cursor line), base32-safe duplicate-anchor guard (`/\s\^tm-\S+\s*$/` — caught+fixed a hex-only regex bug), pure editor edit (no apiFetch/mint client-side). `main.ts` addCommand `tm-create-node-from-selection`. Gate: build + tsc exit 0. Blank-line edge flagged for T6.5 manual TESTPLAN. |
| T6.5 | Clipboard cut/copy carrying anchors + TESTPLAN | DONE | Cursor-built self-contained `clipboard.ts` (pure helpers `lineHasAnchor`/`extractAnchor`/`extractAnchorId`/`collectAnchors` on base32 alphabet `abcdefghijklmnopqrstuvwxyz234567`; `registerClipboard(plugin)` inert wiring point — native Obsidian cut/copy already preserves plain-text anchors; no mint, no API) + `TESTPLAN.md` (manual operator script: Setup, T6.2 persistence, T6.3 status+offline, T6.4 mint w/ E08, T6.5 CUT→MOVE E04 / COPY→E_DUP_ID E05). Per Files list, main.ts NOT edited (registerClipboard unwired by design → SPEC-QUESTION T6.5, resolved MVP-accept). Local gate: build + tsc exit 0; end-to-end TESTPLAN execution is pending-manual (needs live Obsidian+daemon, not fakeable headless). **M6 code-complete; runtime DoD (CI-green + manual TESTPLAN) pending** — see milestone header. |

## M7 — TMS loop (Depends on: M4) — **COMPLETE** (all tasks DONE + verified; 6 spec-questions open)

> **Follow-up T7.2b (2026-07-15):** resolved the elevated SPEC-QUESTION T7.2
> — `store.delete_node`'s S1+ tombstone branch now fires `invalidate`
> (touched = all facet_ids, captured pre-tombstone; called before
> `_reassign_inbound_edges` so subscribers still target the node), so a real
> S1+ retraction (incl. `DELETE /nodes/{id}`) flags bound + `'*'`-bound
> dependents. New `test_s1_node_retraction_flags_dependents` covers the
> tombstone AND redirect paths. Cursor-delegated; gate green (integration
> 109). 7 open → 6 open.

> All seven tasks (T7.1–T7.7) DONE and independently gate-verified (ruff
> clean, pyright 0, unit+property 382, integration 108, battery 27; zero
> tests/golden changes). Milestone DoD command `pytest
> tests/integration/test_tms.py` = 12 passed. **DoD-location nuance:** the
> spec frames the DoD as "test_tms.py covering [4 behaviors]"; by test-tier
> convention the coverage is distributed — supertask-fires-once and
> major-commit-flags-subscribers (via the `revised` cascade) ARE in
> test_tms.py, while `*`-binding-on-any-break lives in unit
> `test_invalidate.py` and split-zero-dangling in property
> `test_split_merge.py`. All four behaviors are covered; not all are
> duplicated into test_tms.py. **7 SPEC-QUESTIONs remain OPEN** (see
> docs/spec-questions.md) — several (reassignment `cause_kind` enum,
> proposal-approval resolution enum, S1+ delete_node→invalidate wiring,
> recheck-schedule persistence) need a product/spec decision or a future
> task, so they are deliberately NOT auto-resolved/archived at close.

| Task | Goal | Status | Notes |
|---|---|---|---|
| T7.1 | Invalidation walk (`invalidate.py`) | DONE | Run 20260714-M7 (parallel cohort w/ T7.7). `invalidate(conn, node_id, commit, touched)` reads subscribers via `store.find_live_edges(dst=node_id)`, filters in Python on `mode=='track'` + `edge_type in JUSTIFICATION_EDGE_TYPES|{'composes'}` + §4.9 facet-binding predicate; damper = non-empty `find_open_reviews(node_id=src, cause_kind='facet_break')`; enqueues via `store.enqueue_review`. 10 holistic unit tests (touched/untouched binding, `*`-on-any-break, damper-dedup, retracted/mode!=track/non-justification ignored, whole-node composes, all-facets retraction analogue). `composes_touched_facet` undefined in spec → narrowest reading (whole-node composes fires on any touched) + SPEC-QUESTION T7.1 (open). Direct edit (worker: ~90 LOC spec-precise, not Cursor). Verify exit 0; independently re-verified (full gate green). |
| T7.2 | Change-class heuristic + wiring into commit | DONE | Run 20260714-M7 (parallel cohort w/ T7.3). `commits.default_change_class(old,new,touched_ids)` → major iff a facet removed/renamed or a touched facet's version bumped, else patch; explicit caller class overrides. `store.commit_node` now calls `invalidate` on major commits INSIDE its `with conn:` txn (rule-0.4 store.py touch outside Files → SPEC-QUESTION T7.2 logged). **Caught a real atomicity bug (advisor-assisted, no test failed): sqlite3 `with conn:` commits on every block exit, so `enqueue_review`'s own `with conn:` would prematurely commit the pending commit → extracted `enqueue_review_within_transaction` (thin wrapper preserves old API); `invalidate` now uses it, composable in a caller txn.** 12 holistic unit tests (heuristic + override + retraction + end-to-end commit_node wiring); T7.1's 10 unregressed. delete_node S1+ retraction still bypasses commit_node → SPEC-QUESTION T7.2 (open, likely closed by T7.5). Direct edit. Verify exit 0; full gate green. |
| T7.3 | Trigger registry + evaluator (`triggers.py`) | DONE | Run 20260714-M7 (parallel cohort w/ T7.2). Closed registry `CONDITIONS` (immutable `MappingProxyType`, exactly the 4 spec'd names) + `_ACTIONS`; `TriggerContext` frozen dataclass; standalone `evaluate(conn, node_id, ctx)` (§4.10a) + `run_daily_tick(conn, contexts)` (§4.10b) — NOT wired into commit_node/store.py (deferred like T7.1's, hard boundary respected: store.py/commits.py untouched). `facet_interface_changed` delegates to `invalidate` (no reimpl); `all_subtasks_closed` enqueues once, never sets task_state, idempotent via find_open_reviews; `evidence_retracted`/`recheck_after` per spec; cause_kind matches store enum. 14 holistic unit tests (each condition ±, sole-side-effect snapshot, closed-registry). `recheck_after` schedule persistence has no DDL → SPEC-QUESTION T7.3 (open, resolve at daily-tick daemon wiring). Direct edit. Verify exit 0 (re-confirmed post-T7.2 refactor); full gate green. |
| T7.4 | Supertask trigger fires once, never auto-closes | DONE | Run 20260714-M7. `triggers.py` needed ZERO change — T7.3 already made `all_subtasks_closed` idempotent (find_open_reviews gate) and non-closing (never writes task_state); confirmed by reading it. Added `tests/integration/test_tms.py::test_supertask_flag`: supertask + 3 composes-linked subtasks; not flagged while one child open; exactly one `subtasks_closed` review once last child closes; supertask task_state stays `open`; re-eval adds no duplicate. **Cursor-delegated** (mechanical: author test vs correct contract). Verify exit 0; facet_span tests unregressed; full gate green (integration 100, battery 27). |
| T7.5 | Review queue: resolutions + daily cap (`review.py`) | DONE | Run 20260714-M7. New `review.py`: `resolve_review` (still_holds/retracted; `revised`→`store.commit_node` so change_class+invalidate CASCADE fires, as two separate top-level txns never nested; `dismissed` REJECTED for non-violation via `DismissalNotAllowedError`), `active_queue` (**READ-SIDE cap 10** — enqueue stays unbounded; ordered `(created_at ASC, inbound_edge_count DESC)`; NULL node_id → inbound 0, avoids `find_live_edges(dst=None)` landmine), `approve_proposal` (parse `cause_ref` envelope via `json.loads` NOT eval; `create_node` once; idempotent — 2nd approval raises `ReviewAlreadyResolvedError`, mints nothing). store.py (rule 0.4): `resolve_review`/`_within_transaction`, `finalize_proposal_approval`, `get_review`. 8 holistic tests that ENCODE the pins (verified: revised-cascade asserts a NEW facet_break; dismiss-rejection; 15-enqueued→10-returned w/ NULL-node proposal; monkeypatched-`_now` tie test for inbound tiebreaker — worker caught + fixed a Cursor test gap). 2 SPEC-QUESTIONs (user-flag ordering; proposal-approval resolution enum). **Cursor-delegated** (judgment pre-resolved in task JSON). Verify exit 0; full gate green (integration 108). |
| T7.6 | Split/merge inbound-reassignment queue | DONE | Run 20260714-M7. Layered on existing split/merge (T1.6): `store.split_node` now enqueues one reassignment review per live inbound edge (KEEPS the eager auto-reassign-to-first-successor safe default — additive human correction, zero-dangling never regresses); `merge_nodes` enqueues none (single unambiguous survivor). New `store.resolve_redirect_chain` (transitive, cycle-safe) + `store.reassign_edge`; `review.resolve_reassignment(conn, review_id, chosen_successor)` follows redirects transitively, re-points the edge, resolves as still_holds (separate top-level txns, no nesting). `tests/property/test_split_merge.py`: Hypothesis st.data() CHAINED split/merge/resolve sequences (later ops target earlier products) asserting zero **transitively**-dangling live edges after each step, + 4 explicit queue tests (exact-K enqueue on split, zero on merge, multi-hop redirect resolution, post-resolve invariant). `cause_kind="reassignment"` has no enum member → SPEC-QUESTION T7.6 (open; needs spec amendment — reuse of any existing member is unsafe). **Cursor-delegated** (pins pre-resolved). Verify exit 0; full gate green (unit+property 382). |
| T7.7 | Facets-from-spans capture (`POST /edges` with `facet_span`) | DONE | Run 20260714-M7 (parallel cohort w/ T7.1). `CreateEdgeBody` gains `facet_span: str\|None`; when present, route mints a facet on `dst` via new `store.mint_facet_from_span(conn, node_id, span, *, author, message)` (id8 via existing `ids.mint`, appends `Facet(version=1)`, `commit_node(change_class="minor")`) then forces edge `facet_binding` to that concrete id (non-`*`). 3 holistic integration tests (`-k facet_span`): mint-on-target + concrete binding + neighborhood visibility + zero spurious reviews; no-span path unchanged; justification-binding invariant satisfied. OpenAPI snapshot regenerated (sanctioned command, not hand-edited) — diff is exactly the new field. `Facet.name` not supplied by capture flow → narrowest reading `name=facet_id` + SPEC-QUESTION T7.7 (open). Direct edit (Facet.name ambiguity + cross-module wiring, not Cursor). Verify exit 0 (both cmds); independently re-verified (full gate green). |

## M8 — Web UI (Depends on: M7) — **CODE-COMPLETE** (all tasks DONE + locally browser-verified; CI leg of the DoD pending first push)

The Playwright smoke test (`tests/integration/test_ui_smoke.py`) drives the full
loop **create → link with span → break facet → see badge → resolve** headless
against a live daemon, deterministically (5/5 **locally**). The DoD says "passes
headless **in CI**" — the `ui-smoke` CI job is wired but has NOT yet run on a
runner (same honest framing as M6: code-complete + locally-green, CI leg pending
first push; the YAML was validated by inspection only). The per-request-connection
fix (T8.5b) was confirmed to reach the **production** daemon path
(`daemon.py::serve` → `create_app(config)` with no injected conn → per-request
`get_conn`), not just the test/harness path.
Tasks: T8.0 (review API, inserted prerequisite), T8.1 (shell), T8.2 (node view),
T8.3 (review view), T8.4 (search+sync), T8.5 (smoke test), T8.5b (concurrency
fix, inserted after the smoke test exposed a data-integrity defect). Every view
was driven in a real browser against the seed harness — **except** T8.4's sync
pause&diff inspector, which is code-verified only (the dev seed registers no sync
root/pause; a richer seed would close it). **All 4 M8 spec-questions RESOLVED
2026-07-18 via fable rulings** (archived to `docs/archived-questions.md`): T8.0
(uncapped `/v1/review` + `node` filter) and T8.1 (guardrailed Files-list
completion; static-shell/no-jinja2 architecture) **ratified**; T8.3
**ratified-with-amendment** — `revised` now shows a disclosed `minor|major`
`change_class` selector (browser-verified) so invalidation blast radius isn't a
silent guess; T8.5b **ratified** — spec §3 amended to per-request WAL
connections + **vision §5 F14** added, plus a concurrent-**write** regression
test. Spec docs updated: §3, §4.11 (`/review` row), §4.13 (rendering + `revised`
disclosure), build-plan (T8.1 Files + Files-list-completion guardrail).

**BINDING M8 rendering architecture (decided at T8.1 via fable design review, 2026-07-17).**
The `.html` files under `src/akasha/ui/templates/` are **static shells served as-is**;
all dynamic server data is rendered **client-side in vanilla `app.js`**, which fetches
JSON from the existing `/v1` endpoints and builds the DOM. **NO jinja2 / no server-side
templating** — adding it would edit `pyproject.toml` (unlisted in every M8 task's Files →
rule 0.8 violation) and clashes with the repo-wide `exec` ban (jinja2 compiles templates
to `exec`'d Python). This is also the only architecture consistent with the T8.2–T8.4
Files lists, which contain only `templates/*.html` + `static/app.js` and **no route `.py`**.
**XSS rule for T8.2–T8.4 workers:** render all server free-text (node bodies, facet text)
via `element.textContent` / `createElement`, **never** `innerHTML` on API data; if a
server-side HTML fragment ever proves unavoidable, it needs a SPEC-QUESTION first and must
route every interpolation through stdlib `html.escape(s, quote=True)`.
NOTE: T8.2+ data fetches from the browser will need the Bearer token wired into `app.js`
(the shell itself is unauthenticated and fetches nothing) — a T8.2 concern.

| Task | Goal | Status | Notes |
|---|---|---|---|
| T8.0 | Review API endpoints (`GET /v1/review`, `POST /v1/review/{id}/resolve`) | DONE | **Inserted + completed 2026-07-17.** Prerequisite for T8.2/T8.3, discovered missing during M8 orientation (§4.11 defines these; T7.5 built only `tms/review.py`; no task's Files list had the route). `routes/review.py` mirrors `nodes.py`: `GET /review` (`require_auth`) returns UNCAPPED `store.find_open_reviews(node_id=node)` — cap-10 is a T8.3 display concern, and an uncapped+node-filtered endpoint is what the T8.2 badge needs (a capped one would mask the bug behind a 1-review seed); `POST /review/{id}/resolve` (`require_human`, ∅) dispatches to `tms/review.py` `resolve_review` with 404/409/409/422 error mapping. No raw SQL (rule 0.4). Delegated to Cursor; caller independently re-verified: ruff clean · pyright 0 · review tests 7 · openapi snapshot 3 (regenerated via sanctioned cmd; diff = exactly the 2 new paths + `ResolveReviewBody`) · full api 54 · unit+property 382 · battery 27. **End-to-end live-driven** via the seed harness: `GET /v1/review?node=<focus>` returns the seeded `facet_break`; `POST resolve` (still_holds) → 200 human-only; re-query → 0. Scoped exactly to the 4 Files. See SPEC-QUESTION T8.0. |
| T8.1 | UI shell + static serving (htmx, no build step) | DONE | Run 20260717-M8. Delegated to Cursor (grok-4.5-high) for the mechanical edits; htmx vendored by worker via curl; independently re-verified by caller (full gate green). `app.py` adds `GET /` → `HTMLResponse(base.html)` with `include_in_schema=False` (keeps `/v1` OpenAPI snapshot golden unchanged — verified: openapi snapshot test stays 3-passed) and NO auth dependency (browser-reachable shell; auth here is per-route, not global). `app.mount("/static", StaticFiles(...))`; both dirs resolved **package-relative** (`Path(__file__).resolve().parent.parent/"ui"`), never cwd. `base.html`: static HTML5 shell, `<title>tm-daemon</title>` (rebrand-clean, grep confirms no product name in `ui/`), refs `/static/htmx.min.js` + `/static/app.js`, `<main id="app">`, nav placeholders. `app.js`: `console.debug("tm ui loaded")`. Vendored `htmx.min.js` (htmx 2.0.4, 50,917 bytes). **No jinja2** (pyproject untouched, verified empty diff). Files-list completions (build-plan Files omitted them; required by Goal/Verify): `static/htmx.min.js` (the "copying static files" asset) + `tests/integration/test_ui_shell.py` (the "lightweight integration test" the Verify demands) — logged as SPEC-QUESTION T8.1. Gate (caller re-run): ruff clean · pyright 0 · ui_shell 3 · openapi snapshot 3 · unit+property 382 · battery 27. |
| T8.2 | Node view | DONE | **2026-07-17.** `GET /node` route (HTMLResponse, `include_in_schema=False`, no auth — mirrors the shell; Files-list completion) serves `node.html` (5 labeled sections + nav, rebrand-clean). `app.js` extended (guarded behind `location.pathname==="/node"`) renders body/facets/1-hop-neighborhood/history + the stale badge, all via `createElement`/`textContent` (never innerHTML). Badge driven by `GET /v1/review?status=open&node=<id>` (the T8.0 endpoint): open `facet_break` → "Stale -- needs recheck"; else `node.vetted` → "Vetted by you" / "Not yet vetted" (R9: never "true"). Token via `localStorage.tm_token` → `Authorization: Bearer` (cross-view pattern). Direct edit. **Caller browser-drove it against the seed harness** and found+fixed a real bug the integration test missed: app.js loads in `<head>` so it ran before `#app` was parsed (getElementById null) → wrapped view-init in a DOMContentLoaded guard. Re-verified in-browser: all 5 elements render, stale badge lights up, XSS canary inert (0 injected `<img>`, present as text), no "true" on page. Gate: ruff clean · pyright 0 · test_ui_node 1 · openapi snapshot 3 · full integration 120 · unit+property 382. Files-list completions: `app.py` route + `test_ui_node.py` (SPEC-QUESTION T8.1 class). |
| T8.3 | Review view (one-click resolutions + daily-cap banner) | DONE | **2026-07-17.** `GET /review` route (mirrors `/node`; Files-list completion) serves `review.html` (`#review-banner` + `#review-queue`). `app.js` `initReviewView` (routed via `boot()` alongside `/node`) fetches `GET /v1/review?status=open` (T8.0, uncapped) and renders each item via `textContent`; per-item one-click buttons POST `/v1/review/{id}/resolve` then refetch+re-render. `dismissed` only rendered when `cause_kind==="violation"` (mirrors server 409 rule). `revised` → minimal inline `<textarea>`+submit (can't be literally one-click — SPEC-QUESTION T8.3). Daily-cap banner shows when open count ≥ 10 (display-only over the uncapped endpoint). Direct edit. **Caller browser-drove it** against the seed harness: queue showed the seeded facet_break review with the correct buttons (no `dismissed`, since not a violation), clicked `still_holds` → resolve→refetch→"No open reviews", server-side confirmed 0 open after; 0 console errors. Gate: ruff clean · pyright 0 · test_ui_review 1 · openapi snapshot 3 · full integration 121 · unit+property 382. |
| T8.4 | Search + Sync views | DONE | **2026-07-17.** `GET /search` + `GET /sync` routes (mirror `/node`; Files-list completions) serve `search.html`/`sync.html`. `app.js` adds `initSearchView` (form → `GET /v1/search?q=` → results via textContent) and `initSyncView` (`GET /v1/sync/status` → per-root files/violations/**pause&diff inspector**/conflicts + unresolved bucket), both routed via `boot()`. Pause diff read from `cause_ref.diff` JSON → `<pre>.textContent` (XSS-safe; traced through `sync/reconcile.py`). "sync root" generic label (Obsidian "vault" copy deferred to M6 thin client, per §4.13). Direct edit. **Caller browser-drove:** search "Earth" → seeded node (canary inert, 0 injected img); sync → graceful "No sync roots registered." empty state. **Verification limitation:** the pause&diff inspector's diff-render is code-verified but not driven with real pause data (the dev seed harness registers no sync root/pause) — a richer seed would close this; not blocking the M8 DoD loop (create→break→badge→resolve is node/review, already proven). Gate: ruff clean · pyright 0 · test_ui_search_sync 2 · openapi snapshot 3 · full integration 123 · unit+property 382 · battery 27. |
| T8.5b | Daemon per-request DB connections (concurrency fix) | DONE | **Inserted + completed 2026-07-17.** Prerequisite for T8.5, discovered when the smoke test drove the node view's 4 concurrent `Promise.all` fetches and exposed a data-integrity defect: the spec-§3 single shared `sqlite3.Connection` corrupts reads under threadpool concurrency (caller reproduced directly: 240 concurrent requests → ~10% 500/401/404, incl. a valid token rejected and an existing node missing). Fix (user-directed "ensure concurrency is possible" → NOT a serializing lock): `deps.get_conn` opens a fresh WAL connection PER REQUEST (concurrent readers + one writer), closed at request end, selected by `app.state.db_path` (injected-connection callers keep the shared path — sequential, safe); `store.connect` gained `PRAGMA busy_timeout=5000`; `app.state.conn` kept for the pre-serving startup reconcile only (`daemon.py`). After fix: 240/240 → 200. Guarded by new `tests/integration/test_concurrency.py`. Amends §3 → **SPEC-QUESTION T8.5b** (open, needs spec ratification). Direct edit (core-correctness, caller-held). Gate: ruff clean · pyright 0 · concurrency test 1 · full suite 533. |
| T8.5 | Playwright smoke test (full loop) | DONE | **2026-07-17.** `tests/integration/test_ui_smoke.py` drives real headless Chromium (`pytest-playwright`) through the full M8 DoD loop against a live uvicorn daemon (served from a `Config(db_path=...)` so it exercises the production per-request path): create target+subscriber via API → `POST /edges` `facet_span` (link with span, T7.7) → `PATCH` major touching the facet (break) → guard: one open `facet_break` on the subscriber → browser: `/node?id=sub` shows "Stale -- needs recheck" badge (R9: no "true") → `/review` click `still_holds` → "No open reviews." + API confirms 0 open. **Was BLOCKED** by the shared-connection concurrency bug (T8.5b) — worker correctly refused to force a green by overriding `get_conn` in the fixture; fixed properly via T8.5b. Added `ui-smoke` CI job (ubuntu: `uv sync` + `playwright install --with-deps chromium` + runs smoke + concurrency tests). Smoke test 5/5 deterministic (~1.16s) **locally**; the `ui-smoke` CI job is wired but hasn't run on a runner yet (DoD's "in CI" leg pending first push). Gate: ruff clean · pyright 0 · full suite **534** (incl. smoke + concurrency). |

## M9 — Hardening (Depends on: M5–M8) — **CODE-COMPLETE** (all tasks DONE; T9.5's literal 24h nightly-soak leg pending first scheduled `main` run)

| Task | Goal | Status | Notes |
|---|---|---|---|
| T9.1 | Windows battery items (CRLF, locking retry, AV noise) | DONE | Run 20260718-054005-M9. Direct Sonnet edit (judgment call on watcher.py-vs-reconcile.py placement, resolved by owning layer). `is_transient_lock_error`/`retry_with_backoff` (generic exponential backoff, PEP 695 generic) added to `watcher.py`; wraps the real OS-level I/O call sites in `reconcile.py` (`write_if_diff`'s existence-read + `os.replace`, `on_change`'s vault-file read) plus `Debouncer.poll`'s re-queue-on-transient-error path. Windows-only trigger (`OSError.winerror` in {5,32,33}) is simulated via a settable attribute since this host is Linux — same accepted pattern as T4.9's `sys.platform`-guarded lock code. New `tests/battery/test_windows.py` (20 tests) monkeypatches real production call sites (`os.replace`, `Path.read_text`) to inject failures and asserts genuine retry/recovery + E09 CRLF confirmation. Verify 20 passed; independently CONFIRMED_DONE (verifier re-ran Verify, read both diffs confirming non-vacuous wiring into real I/O call sites, spot-checked 5/20 test bodies, re-ran tests/unit/sync+tests/property regression slice 86 passed). No SPEC-QUESTIONs. |
| T9.2 | Metrics: RSS/CPU sampling + counters (`metrics.py`) | DONE | Run 20260718-054005-M9. Direct Sonnet edit. New `metrics.py`: every §7 counter (facet_coverage, review_inflow_7d, review_resolved_7d, inflow_variance_30d, violation_rate, auto_repairs{class}, crossing_rate) + stdlib-only cross-platform rss_bytes/idle_cpu_pct sampling (no new dependency — `/proc/self/status` Linux, `ctypes`/psapi Windows, `resource` fallback) + sync_cycle_ms{p50,p95}. New `api/routes/metrics.py` (`GET /v1/metrics`, `require_auth`, registered in app.py). `store.py` gained 7 new READ-ONLY aggregation helpers (rule 0.4 precedent, same as T4.2/T4.5/T5.4/T5.7 — independently confirmed by verifier: no write verbs, no transactions, all genuinely called from metrics.py). OpenAPI snapshot regenerated via the sanctioned command (verifier independently reproduced it bit-for-bit); rebrand invariant holds (no product-name leak). New `tests/unit/test_metrics.py` (26 tests). Verify 26+3 passed; full gate (ruff/pyright/408 unit+property/154 integration) green per worker, independently spot-verified by verifier (test_api.py 54 passed, ruff/pyright clean on touched files). Independently CONFIRMED_DONE. **3 SPEC-QUESTIONs logged** (open, see docs/spec-questions.md): store.py Files-list touch (minor, same recurring precedent); violation_rate/auto_repairs{class}/sync_cycle_ms{p50,p95} have no live producer yet (exposed as an in-process recorder returning 0.0/{} until a future task wires reconcile.py call sites — flagged for T9.5's soak-test author); inflow_variance_30d uses population (not sample) variance, undocumented in spec. |
| T9.3 | S0 GC scheduling + log rotation | DONE | Run 20260718-054005-M9. Direct Sonnet edit. `configure_logging` already used `RotatingFileHandler` but with hardcoded size/backup-count — added keyword-only `max_bytes`/`backup_count` (new `LOG_MAX_BYTES`/`LOG_BACKUP_COUNT` constants, defaults unchanged 10MB/5, backward-compatible). New `GcScheduler` class runs the existing T1.7 `store.gc_objects(conn)` on a background thread (one tick immediately, then every `interval_seconds`, default 24h; own short-lived connection per tick, mirrors `api/deps.py`'s per-request pattern); wired into `serve()` after startup reconcile, inside the single-instance lock, before `uvicorn.run`, stopped/joined in `finally`. Verify 9 passed; full gate 536 passed + battery 27 passed per worker (independently spot-verified: scoped daemon-lifecycle regression slice 13 passed). Independently CONFIRMED_DONE (verifier confirmed GcScheduler genuinely calls store.gc_objects — no reinvented SQL — is genuinely wired not dead code, rotation is real RotatingFileHandler-based, T4.9/T5.6 lock+reconcile logic intact). **SPEC-QUESTION T9.3 logged** (open, see docs/spec-questions.md) — docs/archived-questions.md's T1.7 entry names T9.3 as the home for vision A7's 30-day S0 node-retention GC (age-based node deletion before gc_objects), but T9.3's literal Steps/DoD only describe scheduling the existing object-level gc_objects job; no retention-days config field exists. Implemented the narrowest reading (object-level GC only); node-retention-by-age remains unimplemented pending a human decision. |
| T9.4 | `--dry-run` coverage + error-message pass | DONE | Run 20260718-054005-M9. `--dry-run` was already structurally complete for every mutating verb (all 6 funnel through the shared `_mutate` helper from T4.8) — confirmed via a static scan across every registered typer command, no verb bypassed it. Real bug found+fixed: `_usage_error` (client-side validation, exit 2) ignored `--json` and always printed plain text, unlike `_fail` (server errors) — threaded `state` through it and its two callers (`_parse_facets`, `token_create`) so client- and server-side rejections are both consistent under `--json`. Minor clarity fix: `E_CONNECTION` now names the unreachable `--base-url` explicitly. New `tests/integration/test_cli_dry_run.py` (19 tests): table-driven enumeration cross-checked against every statically-discovered mutating verb (fails if a future verb skips `_mutate`), dry-run behavior (plain + `--json`) per verb, flag-order independence, `_usage_error` `--json` regression. Verify 19 passed; independently CONFIRMED_DONE (verifier re-ran Verify, confirmed both files exist/non-empty, cross-checked git status). No SPEC-QUESTIONs. |
| T9.5 | 24-hour soak test | DONE | Run 20260718-060500-M9. Direct Sonnet edit. New `tests/battery/soak.py`: injectable `Clock` protocol (`RealClock` used by both invocations; `FakeClock` documented escape hatch) drives one tick loop shared by the in-session accelerated proxy (`--hours 0.05`, ~3 min, used as this task's runnable Verify) and the literal 24h nightly run — same "code-complete, CI-leg pending first push" framing as M0/M6/M8's runtime legs. Each tick drives real production traffic (direct `store.py`+`reconcile.py` for vault-file bootstrap, then the real FastAPI app via `TestClient` for node/edge/review/search/rescan), samples via `GET /v1/metrics` (T9.2's real `compute_metrics`, not reimplemented), scans real JSON-line logs for ERROR/CRITICAL, and fails fast (non-zero exit) on RSS≥150MB, mean idle CPU≥30%, or any unhandled exception — not just a printed warning. `soak.py` is correctly excluded from pytest collection by filename alone (no pytest.ini/conftest.py in repo; `tests/battery` still 47 passed). New `nightly-soak` CI job in `.github/workflows/ci.yml` runs the literal `--hours 24` command, gated `if: github.event_name == 'schedule'`. Verify (accelerated proxy) passed: RSS ~63MB, idle CPU ~0.4%, 0 unhandled exceptions. Independently CONFIRMED_DONE — verifier read the full script confirming no stubbed assertions/sampling, re-ran Verify + full gate independently, confirmed `metrics.py`/`reconcile.py` scope guard held. Verifier caught a real bug: the new `schedule:` trigger had no `if` guard on the 4 pre-existing jobs, which would have caused the whole push/PR suite to silently duplicate every night — fixed by the caller in the same in-scope file (`ci.yml`) before closing, re-validated YAML + re-ran full gate (562 unit+property+integration, 47 battery) after the fix. **All 5 M9 tasks are now DONE; M9 — Hardening is CODE-COMPLETE (T9.5's literal 24h nightly-soak CI leg is pending its first scheduled `main` run, same framing as M6/M8).** |

## M10 — Dogfood instrumentation (Depends on: all)

| Task | Goal | Status | Notes |
|---|---|---|---|
| T10.1 | Metrics dashboard view | DONE | Run 20260718-180539-M10 (Path B, direct Task-tool dispatch). Prior overnight run left `dashboard.html`/`app.js` ~90% complete but stalled stop-and-logging the one remaining gap (no `GET /dashboard` route in `app.py`) as an out-of-scope file rather than applying the T8.1-T8.4/T9.2 Files-list-completion precedent. This run's fleet-worker was explicitly authorized to take spec-question T10.1's option (a): added the one-line `GET /dashboard` route to `app.py` (byte-for-byte mirrors `/node`/`/review`/`/search`/`/sync`, `include_in_schema=False`, no OpenAPI snapshot impact — confirmed via `test_openapi_snapshot.py` still 3 passed), simplified `tests/integration/test_ui_dashboard.py` to drop its test-local route workaround and drive the real route, and resolved the `docs/spec-questions.md` T10.1 entry. Verify (`tests/integration/test_ui_dashboard.py`, real headless-Chromium Playwright test) 1 passed. Independently CONFIRMED_DONE by a separate fleet-verifier (re-ran Verify + openapi snapshot test, read-through confirmed the route is genuine and the workaround is gone, git status matched claimed files). Caller re-ran the full gate directly: `make check`-equivalent (ruff/pyright/408 unit+property) green, `tests/battery` 47 passed, full `tests/integration` 155 passed. Logs: `docs/agents/logs/20260718-180539-M10/{workers,verify}/T10.1/`. |
| T10.2 | Export command (`akasha export --md DIR`) | DONE | Run 20260718-180539-M10 (Path B). Build-plan Goal ("render every managed node to markdown") had no answer without either inventing an HTTP endpoint (rule 2) or breaking the CLI's pure-HTTP-client architecture (T4.8) — flagged as 2 spec-questions (transport, scope) before implementation, resolved same-day by a fable-model spec ruling (`docs/spec-questions.md`, `docs/mvp-spec.md` §4.11/§4.12, `docs/build-plan.md` T10.2 all amended; see commit `7eb34b6`). Ruling: new read-only `GET /v1/sync/export` on the existing `routes/sync.py` router (CLI stays pure-HTTP, matching vision §7.11 API-first-parity + §7.12 rule 1 migration-boundary invariants); scope = sync-projection-tracked nodes (`store.list_sync_files` rows with a base snapshot) with a disclosed `unfiled_node_count` for anything outside that projection (vision invariant 5, never silently included/dropped). Implementation: endpoint reuses the exact `parse`+`hub_state_for`+`render` projection the sync write-back path already uses internally (T5.8-proven byte-stable); `hub_state_for` gained a `read_only=True` mode (existing `Reconciler.on_change` call sites unaffected) so the GET enqueues zero review-queue writes; new `store.list_live_node_ids` read-only helper (rule-0.4 completion, T9.2 precedent); `cli export --md DIR` is a pure HTTP client writing each item's canonical text byte-for-byte; OpenAPI snapshot regenerated (additive-only diff, confirmed idempotent on a second regen). First implementation attempt was cut off mid-task by an account usage-window limit before touching any files (same failure mode the M9/M10 overnight-loop handoff diagnosed) — re-dispatched cleanly from scratch. Verify (`tests/integration/test_export.py tests/integration/test_openapi_snapshot.py`) 12 passed. Independently CONFIRMED_DONE by a separate fleet-verifier (re-ran Verify + full gate, read-through confirmed the read-only gate is a real conditional not a no-op flag, confirmed tests are non-vacuous — byte-for-byte content assertions, a genuine read_only-suppression control test, a real unfiled-node scenario, re-export-byte-stability actually exercised — and confirmed the snapshot diff is additive-only). Caller re-ran the full gate directly: ruff/pyright/408 unit+property green, `tests/battery` 47 passed, full `tests/integration` 169 passed. Logs: `docs/agents/logs/20260718-180539-M10/{workers,verify}/T10.2/`. |
| T10.2b | Contradiction surfacing at capture (story 2, non-LLM) | DONE | Run 20260719-152301-M10 (Path B). Task itself was *created* 2026-07-19 by a fable ruling after T10.3's acceptance audit found PRD §8 story 2 (non-LLM contradiction/near-duplicate surfacing on capture) was never built — no M7 task produced it and §9 row 2 cited a nonexistent test (see `docs/spec-questions.md` T10.3 story-2 gap; spec amended in commit `1341483`). Implementation: read-only `store.find_contradiction_candidates(conn, node_id, body, *, limit=5)` (rule-0.4 completion, T9.2/T10.2 precedent) — canonicalizes+tokenizes the new claim body to alphanumeric terms (empty set ⇒ `[]`, no MATCH issued: the FTS5-syntax-hostile-body defense), FTS5-quotes/OR-joins, queries the **existing** `nodes_fts` index joined to `nodes` filtered `type='claim' AND status='live'`, excludes the new node, ranks exact-duplicate-first via an explicit `(is_exact, bm25)` key, caps 5, attaches evidence via the existing `find_live_edges(src=cand, edge_type='cites')` filtered to `node_type=='evidence'`. Zero writes, no transaction, no new schema/index/`cause_kind` (PRD §5 F-list: no LLM in the truth path). `routes/nodes.py`'s `create_node` computes the additive `contradiction_candidates` field only on the human 201 path (agent branch returns the 202 proposal before the new code). OpenAPI snapshot regenerated but produced **no diff** — `/nodes` returns untyped `dict[str,Any]` (no `response_model`), so an additive runtime key has no schema-level surface; verifier independently re-ran the regen and confirmed byte-identical (not a skipped step). Verify (`tests/integration/test_contradiction_surfacing.py tests/integration/test_openapi_snapshot.py`) 9 passed. Independently CONFIRMED_DONE by a separate fleet-verifier (re-ran Verify + independent openapi regen; read-through confirmed the helper is genuinely read-only + FTS5-safe + correctly filtered/ranked, the route change is confined to the human 201 path, and all 6 tests are non-vacuous — real body/evidence/date assertions, a genuine byte-identical DB-state read-only proof, a genuinely hostile FTS5 body asserting 201-not-500, and an agent-path no-candidates-leak check). Caller re-ran the full gate directly: ruff clean, pyright 0 errors, `tests/unit tests/property tests/integration` 583 passed, `tests/battery` 47 passed. 1 non-blocking SPEC-QUESTION logged (open, narrowest reading taken): "Evidence-type" undefined against the closed NodeType enum — shipped `node_type=='evidence'` only (excludes `proof`), see `docs/spec-questions.md` T10.2b. Logs: `docs/agents/logs/20260719-152301-M10/{workers,verify}/T10.2b/`. |
| T10.3 | Acceptance mapping (`docs/acceptance.md`) | TODO | |
