You are a fleet-worker executing build-plan task T10.2 (Export command). This task was re-specified today (2026-07-18) by a fable-model spec ruling after the human operator resolved two architectural ambiguities — read `docs/spec-questions.md`'s two "## T10.2" entries (transport, scope) and `docs/build-plan.md`'s current T10.2 entry in full before starting; they are the authoritative task definition, more current and more detailed than anything you might recall from an older copy of the build plan. Do not re-litigate the transport/scope decisions — they are settled. Nothing has been implemented for this task yet (a prior worker attempt was cut off by an account usage-limit before touching any src/tests files — confirmed via git status, nothing to resume); you are starting from scratch.

## Settled decisions (do not re-open these)

1. **Transport:** `export` is a new read-only `GET /sync/export` endpoint on the *existing* `src/akasha/api/routes/sync.py` router (no `app.py` change needed — that router is already included in `app.py`). The CLI's `export` verb stays a pure HTTP client of it (same as every other verb except `daemon`). This is a REAL, visible API-surface addition (not a UI-shell route), so it DOES need `include_in_schema` left at its default (schema-visible) and the OpenAPI snapshot regenerated in the same change — this is unlike T10.1's dashboard route.
2. **Scope:** "every managed node" = every node currently tracked in the sync projection (i.e. every path in `store.list_sync_files(conn)` that has a base snapshot), matching the codebase's existing "managed" terminology. Nodes never linked to any sync root are reported via a separate `unfiled_node_count`, never silently included or silently dropped.

## Goal / Files / Steps / Verify / DoD (from `docs/build-plan.md`, current T10.2 entry — re-read it yourself for the authoritative text)

- Goal: Export the hub to markdown in a target directory, via a new read-only endpoint; the CLI stays a pure HTTP client.
- Depends on: T4.8, T3.3 (both DONE).
- Files: `src/akasha/api/routes/sync.py` (new `GET /sync/export` route), `src/akasha/kernel/store.py` (new read-only helper for `unfiled_node_count` — a rule-0.4 Files-list completion, same precedent as T4.2/T4.5/T4.6/T9.2's read-only store.py additions), `src/akasha/sync/reconcile.py` (add a read-only mode to `hub_state_for` — see gotcha #1 below), `src/akasha/cli/main.py` (`export` verb), `docs/api-snapshot/openapi.json` (regenerate in this same change), `tests/integration/test_export.py` (new), `tests/integration/test_api.py` (add endpoint coverage for `GET /sync/export`).
- Verify: `uv run pytest tests/integration/test_export.py tests/integration/test_openapi_snapshot.py`
- DoD: export writes canonical markdown for every managed projection; re-export is byte-stable; `GET /sync/export` mutates nothing (review queue identical before/after the call, asserted in a test); OpenAPI snapshot gate green.

## Technical gotchas (already investigated for you — use this, don't rediscover it)

1. **`hub_state_for` has a mutating side effect you must suppress.** `src/akasha/sync/reconcile.py`'s `hub_state_for(conn, structure, *, path=None)` (around line 647) calls `store.enqueue_review(conn, node_id, "violation", ...)` when it finds a hub body with an internal newline (unprojectable by the line-oriented grammar). A GET endpoint must mutate NOTHING, not even a review-queue insert. Add a keyword-only parameter (e.g. `read_only: bool = False`) that, when `True`, skips the `enqueue_review` call but still keeps the block's original skeleton text for that entry (identical behavior otherwise — do not change what text ends up in the render, only suppress the DB write). Thread this through to the export endpoint's call site. Write a test asserting `store.find_open_reviews(conn)` (or an equivalent count) is identical before and after calling the export endpoint, including in a scenario with an unprojectable body, to prove the read-only mode actually works and isn't dead code.
2. **The projection recipe (already used internally, you're just exposing it over HTTP):** for each row in `store.list_sync_files(conn)` that has a base snapshot (`store.read_base_snapshot(conn, sync_root_id, path)` is not `None` — skip `None` ones, same as `sync.reconcile.ProjectionIndex.build`'s own skip rule), do `parse(base_text)` (from `akasha.contract.parser`) to get the skeleton `BlockSet`, then `hub_state_for(conn, skeleton, path=path, read_only=True)` to project current hub state onto it, then `akasha.contract.render.render(...)` to get canonical text. `list_sync_files` rows already carry `sync_root_id` and a root-relative `path` (it's the table's primary key — confirmed by reading `store.py` directly, no additional join needed beyond `store.list_sync_roots(conn)` to map `sync_root_id` -> root `name` for the response/output-directory grouping).
3. **Response shape** (per the mvp-spec.md §4.11 amendment, re-read it yourself at `docs/mvp-spec.md`'s `GET /sync/export` row): items ordered by `(sync_root name, POSIX root-relative path)`, each `{sync_root, relative_path, text}` where `text` is the canonical render; a top-level `unfiled_node_count` = count of live (non-tombstoned) nodes present in NO managed projection (i.e., whose id never appears in any parsed base snapshot's block ids across all sync files). Any authenticated token class (not human-only) — this is a read with no write, so it doesn't need `mutation_gate`/proposal-rewriting either (same reasoning already documented in `routes/sync.py`'s own module docstring for `GET /sync/status`).
4. **CLI verb:** `akasha export --md DIR` fetches `GET /sync/export`, and for each item writes `text` byte-for-byte to `DIR/<sync_root>/<relative_path>` (creating parent directories as needed), then prints a `--json`-compatible summary (files written + `unfiled_node_count`) using the same `cli/v1` envelope / `_echo_ok` pattern every other verb in `cli/main.py` uses. Re-export byte-stability follows for free from the endpoint always serving the canonical render (already proven byte-stable by T5.8) — just make sure the CLI doesn't itself transform `text` (no re-encoding, no added/stripped newlines) before writing the file.
5. **OpenAPI snapshot regeneration:** the regenerate command is documented in `tests/integration/test_openapi_snapshot.py`'s module docstring (`uv run python -m tests.integration.test_openapi_snapshot`) — run it after adding the route so `docs/api-snapshot/openapi.json` reflects the new endpoint, then confirm `tests/integration/test_openapi_snapshot.py` passes (this is part of your Verify command).

## Non-negotiable rules (root CLAUDE.md)

Never invent schema/endpoints/grammar beyond the spec — in this case the endpoint and CLI verb are ALREADY spec-sanctioned by today's fable ruling (`docs/mvp-spec.md` §4.11/§4.12 already amended), so just implement exactly what's specified there, don't add anything beyond it. Never edit golden files/fixtures to make tests pass. All persistent writes go through `src/akasha/kernel/store.py` (this task adds zero new writes — it's entirely read-only, which is the whole point). No pickle/eval/exec anywhere. Touch only the Files listed above (plus the OpenAPI snapshot, which is a generated artifact, not hand-edited).

If you hit a genuine NEW ambiguity not already covered by the fable ruling or the gotchas above, follow the standard spec-ambiguity procedure: add a `# SPEC-QUESTION:` comment, draft (don't write) a `docs/spec-questions.md` entry, and return `status: "BLOCKED"` with the draft in `spec_questions` — do not guess past it.

If you have not reached a terminal status (DONE or BLOCKED) within roughly 30 tool calls (this is a bigger, multi-file task than a typical single-file one), stop immediately and report status BLOCKED with blocked_reason "possible hang — exceeded tool-call budget". Do not continue indefinitely.

Return your result via the required structured schema. files_changed must be the actual output of `git diff --name-only` plus untracked files you created (check with `git status --porcelain`), not a guess. End your reply with a fenced ```json block containing exactly: status, files_changed, verify_command, verify_exit_code, verify_stdout_tail, spec_questions (array, empty if none), blocked_reason (only if BLOCKED).
