T9.3b — S0 node-retention-by-age GC (vision A7). Add Config.s0_gc_retention_days (default 30, TOML-loaded), a read-only store.py helper list_expired_s0_node_ids(conn, older_than_iso) over existing nodes.created_at/maturity columns (no schema change), and wire GcScheduler's tick to delete expired S0 nodes via the existing store.delete_node S0 hard-delete branch before calling store.gc_objects in the same tick. Never touch S1+ nodes. Files: config.py, kernel/store.py, daemon.py, tests/integration/test_gc_schedule.py. Verify: uv run pytest tests/integration/test_gc_schedule.py. Full detail: docs/build-plan.md "### T9.3b".
