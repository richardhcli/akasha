Independently verify T9.3b's claim. Re-run tests/integration/test_gc_schedule.py and the full regression sweep; confirm store.py's new helper is genuinely read-only and gc_objects unchanged; confirm daemon.py orders node-deletion before gc_objects in the same tick and only ever targets S0-scoped ids; confirm the S1+-node-survives test is real (genuine inbound edge established and maturity=='S1' asserted before aging, real scheduled tick, real post-tick survival assertions) -- the single most safety-critical assertion in the task. Report CONFIRMED_DONE, CONTRADICTS_CLAIM, or INCONCLUSIVE.
