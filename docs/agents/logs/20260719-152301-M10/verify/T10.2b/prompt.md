You are an independent verifier for build-plan task **T10.2b — Contradiction surfacing at capture (story 2, non-LLM)**. You did NOT do the work. Your job is to catch a worker that claims success without having actually done it — trust nothing below except as a claim to check against real on-disk/git state and your own command runs.

## Background so you judge correctness, not just presence
T10.2b was registered today by a fable ruling. Authoritative design: `docs/build-plan.md` `### T10.2b` entry + `docs/mvp-spec.md` §4.11's "Contradiction surfacing at capture" paragraph (read both). Settled facts: (1) human-token `POST /v1/nodes` 201 responses carry an additive `contradiction_candidates` list (`[]` unless created node `type=="claim"`), computed by a NON-LLM FTS5 heuristic over the EXISTING `nodes_fts` index; (2) strictly READ-ONLY — the surfacing writes nothing (no review item, no proposal, no row); (3) agent-token path stays a 202 proposal with no candidates; (4) no new schema/index/table/`cause_kind`.

## The worker's claim
- status=DONE; files_changed=["src/akasha/kernel/store.py", "src/akasha/api/routes/nodes.py", "tests/integration/test_contradiction_surfacing.py"].
- Added read-only `store.find_contradiction_candidates(conn, node_id, body, *, limit=5)`: canonicalize+tokenize body to alphanumeric terms (empty ⇒ `[]`, no MATCH), FTS5-quote/OR-join, `nodes_fts MATCH` joined to `nodes` filtered `type='claim' AND status='live'`, exclude the new node, explicit `(is_exact_match, bm25)` sort so byte-equal body ranks first, cap 5, evidence via `find_live_edges(src=candidate, edge_type='cites')` filtered to `node_type=="evidence"` (narrowest reading, flagged inline). Claims zero writes / no transaction.
- `routes/nodes.py`: human 201 path computes the field; agent 202 branch untouched (returns before the code).
- **`docs/api-snapshot/openapi.json`: claims regeneration produced NO diff** because `/nodes` returns untyped `dict[str, Any]` (no `response_model`), so FastAPI's schema has no field-level detail to change. THIS IS THE CLAIM MOST WORTH CHECKING — verify it, do not take it on faith.
- New `tests/integration/test_contradiction_surfacing.py`: 6 tests (exact-dup ranks first w/ evidence text+date; near-dup surfaces; non-claim ⇒ `[]`; agent ⇒ 202 no candidates; FTS5-hostile body ⇒ 201 no 500; read-only-gate snapshotting nodes/commits/edges/review_queue byte-identical before/after).
- Claims full gate green: ruff clean, pyright 0, `tests/unit tests/property tests/integration` 583 passed, `tests/battery` 47 passed.

Verify command to re-run yourself: `uv run pytest tests/integration/test_contradiction_surfacing.py tests/integration/test_openapi_snapshot.py`

## Steps
1. Run the Verify command yourself. Record REAL exit code + output tail (worker claims 9 passed).
2. `git status --porcelain` + `git diff --name-only` — confirm exactly the 3 claimed files changed, no unclaimed source edits, no claimed-but-absent file. (Pre-existing unrelated items — a deleted `docs/agents/temp/handoff-*.md` and an untracked `.cursor/*` file — are NOT this task's; ignore them.)
3. **Verify the openapi no-diff claim independently:** run `uv run python -m tests.integration.test_openapi_snapshot` yourself, then `git status --porcelain docs/api-snapshot/openapi.json`. If regeneration leaves the snapshot unchanged AND `tests/integration/test_openapi_snapshot.py` passes, the claim holds (a genuinely additive runtime-only key with no `response_model` schema impact — acceptable, DoD "snapshot gate green" is satisfied). If regeneration DOES produce a diff the worker failed to commit, that's CONTRADICTS_CLAIM.
4. Read `find_contradiction_candidates` in `store.py` in full. Confirm: genuinely READ-ONLY (no INSERT/UPDATE/DELETE, no `with conn:` transaction, no enqueue/commit); empty-term-set returns `[]` without issuing a MATCH (the FTS5-hostile-body defense is real, not claimed); terms are FTS5-quoted (no raw-body MATCH that would raise OperationalError); the new node's own id is excluded; filter is `type='claim' AND status='live'`; exact (byte-equal canonical body) ranks first via an explicit key, not left to bm25 alone; cap 5; evidence pulled via the existing `find_live_edges`/`cites` surface (no reinvented SQL).
5. Read the `create_node` change in `routes/nodes.py`. Confirm the field is computed ONLY on the human 201 path and the agent branch (`ctx.token_class == "agent"` → `_proposal_response`) is genuinely untouched / returns before the new code.
6. Read `test_contradiction_surfacing.py` in full. Confirm it is NOT vacuous: real content assertions (the exact-dup test asserts the existing claim's body text AND its evidence node's body text AND a date field actually appear — not just a non-empty list); the read-only-gate test truly snapshots real table state (nodes/commits/edges/review_queue) before and after and asserts byte-identical; the FTS5-hostile test uses a genuinely hostile body (quotes/operators/punctuation-only) and asserts 201 + a list, never 500; the agent-token test asserts 202 with no candidates leakage.
7. Run the full gate yourself: `uv run ruff check src tests && uv run pyright src && uv run pytest tests/unit tests/property tests/integration -q` and separately `uv run pytest tests/battery -q`. Record REAL results.
8. Verdict:
   - **CONFIRMED_DONE** only if: your Verify run exits 0; exactly the claimed files changed; the openapi no-diff claim holds under your independent regen; the helper is genuinely read-only + FTS5-safe + correctly filtered/ranked; the route change is confined to the human 201 path; the tests are non-vacuous; and the full gate (ruff/pyright/unit/property/integration/battery) is genuinely green.
   - **CONTRADICTS_CLAIM** if the worker claimed DONE but any check fails (e.g. a hidden write in the helper, a raw-body MATCH that can 500, a vacuous test, an uncommitted openapi diff, or a failing gate).

If you have not reached a terminal verdict within ~25 tool calls, stop and report notes explaining why.

End your reply with a fenced ```json block containing exactly: files_exist (array of {path, exists, nonempty}), verify_exit_code, verify_stdout_tail, git_status_matches_claim (boolean), verdict, notes.
